# CURRENT STATE

> **Supersedes all prior state documents and mirrors.**
>
> **Version:** v21 — July 11, 2026 night shipping cut
>
> **Owner:** Corey Hartin (Branch.io Strategic Account Executive; Day 1 = July 13, 2026)
>
> **Authority:** This repository is the master system of record. Todoist is the action layer. Google Drive is the human-readable mirror.
>
> **Source:** Seeded from Todoist task `6h54JCgmg8xRp2Vg`; supersedes Drive v20 (`1PeTg6kZ71G1ZgqrCp9X5W-GSVD1bz8BZCfH0Fppv9E0`).

## HOW THIS FILE WORKS

### SESSION START — ALL AGENTS, EVERY SESSION

Claude, Codex, and Cowork must read **CURRENT STATE** and scan the most recent **SESSION LOG** entries before acting. This file is authoritative; Todoist is the task layer; Drive is a read-only human mirror.

### SESSION END — ONLY AFTER A SYSTEM-STATE CHANGE

If a session shipped code, deployed, changed production data or configuration, or altered architecture, record that state under these rules:

1. **Use the work PR.** Refresh **CURRENT STATE** and add the dated **SESSION LOG** entry to the same branch and PR as the work that caused the change, before that PR merges. Reference the PR number and the latest substantive branch-head SHA known before the state/log commit; never reference the post-merge commit SHA.
2. **Never open a standalone log PR.** A PR whose only purpose is a state or log entry is prohibited.
3. **Batch non-PR state changes.** Production deploys, production database edits, verification, and other state changes without their own repository PR must fold into the next repository PR's entry or be batched into an already-open work PR at session end—never one PR per event.
4. **Keep immutable history.** Use the heading `## YYYY-MM-DD — Author — Title` with **Who**, **What**, and **SHAs**; place the newest entry first and never edit, delete, or reorder prior entries.

Read-only or advisory sessions that changed nothing do not add a log entry.

### WRITE CAPABILITY

Codex and Claude write this file through the repository. Claude drafts entries and hands them to Codex to include in the relevant work PR when Claude cannot push. Cowork mirrors the state to Drive, but the repository remains the source of truth. Log state changes, not attendance.

## DYNAMIC — MASTER STATE & ROADMAP

**v21 — JUL 11 NIGHT SHIPPED: STRATEGY CITATION INTEGRITY + COMPETITIVE RETRIEVAL LIVE IN PROD**

The single source of truth for the entire app.

**Updated:** July 19, 2026 (v21 — private TOC authoring pre-access invocation
guards hardened in draft; initialization and migration/runtime baseline unchanged)

**Supersedes:** v20 (`1PeTg6kZ71G1ZgqrCp9X5W-GSVD1bz8BZCfH0Fppv9E0`). v20 recorded the corpus/fluency scale-up and listed the STRATEGY six-layer rollout as PENDING. v21 records that several of those layers shipped to prod the night of Jul 11 and are verified live.

### §A. WHAT SHIPPED JUL 11 (NIGHT) — DELTA OVER v20

1. **STRATEGY PROMPT CONSOLIDATION (L0) — SHIPPED.** ~56K-character prompt consolidated to <17,500. Twenty-three precedence conflicts resolved and documented; five DECISION rows equivalence-checked. PR #4 (merge `040751b`).
2. **COMPETITIVE + INDUSTRY WIRING (L6) — SHIPPED.** `competitive_intel` (CARD) and `vertical_briefs` wired via situation classifier → `situationIntelligenceRetrieval` → evidence packet, gated and fail-soft. Closes v20's “Strategy-wiring pending.”
3. **CITATION UI + INTEGRITY (L4) — SHIPPED & HARDENED.** Citations render (`RESOURCE`/`KI`/`CARD`/`PLAYBOOK`). The CARD namespace was restored after being dropped. The fabrication guard is live: a citation naming a non-retrieved source renders ⚠ UNVERIFIED plus an audit notice—verified in prod. Source-claim integrity requires source-derived facts to carry a tag; reasoning and opinion remain untagged. The Sources panel is rendering. PR #6 (merge `7ee1ebe6`, commit `41b539ed`).
4. **CLASSIFIER EXPLICIT-INTENT OVERRIDE — SHIPPED.** Explicit competitive asks now deterministically trigger retrieval; the grammar is qualified so “beat my number” and “replace this paragraph” do not over-trigger. Fixes the `classifier_not_requested` → “none in evidence” failure. PR #6.
5. **COUNT DETECTION (#3/#13) — SHIPPED.** `explicitOutputRequest.ts`; explicit counts win for general plural nouns, while durations such as “4 business days” are rejected. PR #4 stack.
6. **IDENTITY → TERRITORY (#12) — SHIPPED + DATA CONFIRMED.** Identity comes from `territory_profile`, not hardcoded values. Prod confirmed: Strategic Account Executive / 13 accounts / $1.4M USD Expansion / verticals media-ent, travel, retail, finserv. Stale “Senior AE / 14 accounts” text was removed.
7. **FRONTEND TYPE DEBT — SHIPPED.** PR #5 fixed 28 `RejectExcessProperties` errors; the postinstall type-safety bypass was removed; typecheck now compiles project references.

**DEPLOY STATE:** Edge functions (`strategy-chat`, `analyze-call`, `mcp`) were deployed via `supabase functions deploy` from merged `main`; `strategy-chat` is live from `41b539ed`. The frontend was published. Production Supabase remains `odbjjklumdsuqdvkgwyv` (Lovable-managed)—not migrated (§D).

### §B. STRATEGY SIX-LAYER STATUS

- **(0) Prompt consolidation:** SHIPPED (budget 17,491/17,500).
- **(1) Deterministic retrieval plan:** Live.
- **(2) Industry (`vertical_briefs`) wiring:** Live.
- **(3) Product wiring:** NOT scoped (gap unchanged).
- **(4) Citation UI:** SHIPPED + integrity hardening.
- **(5) Web Research widening:** IN FLIGHT (`agent/web-research-widening`; reconcile onto new `main`).
- **(6) Competitive wiring:** SHIPPED (cards reach the model; relevance is noisy—S-R1).

### §C. VERIFICATION PROVENANCE (JUL 11 NIGHT)

- Code claims verified against production `main` by direct repository read: SHAs, scope, call-site wiring, classifier grammar, and evidence-policy line.
- CI green confirmed via GitHub check-runs on PR #6 head (`41b539ed`).
- Fabrication guard and competitive retrieval verified live in production (screenshotted): ⚠ UNVERIFIED-CARD fired; the real Adjust card reached the model.
- `territory_profile` confirmed in the production database via Lovable read.
- Budget 17,491 came from the Codex Deno matrix rerun; it was not independently rerun in the originating session because of a private registry. Risk assessed as low.
- Todoist roadmap (`6gwh7jGXpJRJXhpg`) has a completed entry plus S-R1, S-R2, and S-R3.

### §D. OPEN / NEXT

#### New (Jul 19)

- **PRIVATE TOC ANNOTATION PRE-ACCESS GUARDS — PR #24 OPEN/DRAFT/UNMERGED; INITIALIZATION UNRUN/BLOCKED.** Draft PR #24 at latest substantive head `e13c5acdaf13e80bd7fe31a3eaa59fff55d21684` adds one consolidated side-effect-free action-invocation validator after parsing but before execution-tool validation, any private/capture path opening, durable lock acquisition, directory creation, capture read, or checkpoint load. Initialization now requires generation 0, all-zero head/release token, identical primary/current operator, and empty finalization authorization. Every later action requires a positive generation and nonzero lowercase 64-hex head/token; non-peer actions require the current operator to equal the primary operator, while peer review requires a case-insensitively distinct operator. Finalize alone requires `CREATE_UNVALIDATED_LEDGER`; inspection/capture checkout bindings must be lowercase 40-hex before private access. Existing post-open package/checkpoint identity and history checks remain defense in depth. Planted invalid tuples prove zero private operations and unchanged synthetic roots/capture metadata with fixed nonleaking diagnostics. Substantive-head CI run `29688934743` passed Linux/PostgreSQL 17, macOS, and PostgreSQL 17.6/18.4 compatibility; final-head CI remains live GitHub metadata and must be checked directly. Initialization has not run and remains blocked pending merge, a fresh merged-main preflight, and separate explicit authorization. Validation, restore planning, restore commands, and migration readiness remain `BLOCKED`/`RED`. No private artifact, annotation root, export, Lovable, Supabase, n8n, database, target, secret, deployment, or runtime was accessed or changed.

- **PRIVATE TOC ANNOTATION AUTHORING — PR #23 MERGED; AUTHORING NOT RUN AND ALL DOWNSTREAM GATES BLOCKED.** PR #23 merged with history preservation at main `1b0116a09d05a9f787c27d9a24a2f021fcecca0f`; final reviewed head `59b10829b2bf9c915e8e64bb13b6ab70fe484940` contains latest substantive commit `4d6c2a61257528a36f7a01cad1d12ddad7167bcf`. Its reviewed local-only authoring, role-specific TTY review, immutable checkpoint/resume, correction/invalidation, peer-review, and separate unvalidated-ledger finalization boundaries remain as recorded below. Authoring has not run against private evidence. A generated ledger would remain `REVIEW_REQUIRED`; later validation can produce at most `ELIGIBLE_FOR_HUMAN_REVIEW`. Validation, restore planning, restore commands, and migration readiness remain `BLOCKED`/`RED`.

#### New (Jul 18)

<!-- Superseded mutable PR #23 draft wording removed after its history-preserving merge; the Jul 19 merged-state bullet above is authoritative, and the immutable session entries below retain the reviewed implementation history. -->

- **BOUNDED `pg_restore` CAPTURE CONTAINMENT — PR #22 MERGED; ALL ANNOTATION, RESTORE, AND MIGRATION GATES BLOCKED.** PR #22 merged with history preservation at main `c78a0e09373235fe38d045c2bfa7a0fd8a32d364`; final reviewed head `10bbf82b907f880eaa9943ca2ce07fb8406e3dd0` contains the wrapper correction at `9fda6565a49712bae89effd848c31be73e8fb379` and later synthetic fixture/STATE corrections. Every bounded `pg_restore` invocation now requires exactly one validated owner-private mode-`0700` parent. The production envelope passes its already held staging-directory descriptor through the low-level capture into the wrapper; the direct metadata inspector separately holds its private work-directory descriptor. Successful stdout/stderr bytes remain private until the exclusive captures are descriptor-relatively removed and the parent directory is fsynced. Reap, close, unlink, or fsync ambiguity emits no successful bytes and either cleans completely or remains only inside the reviewed private indeterminate/quarantine boundary; it cannot publish an ordinary complete capture package. Planted synthetic failures prove raw TOC bytes existed before cleanup failure, no top-level system-temp wrapper directory appeared, no sentinel reached visible diagnostics, the canonical synthetic ZIP and a complete planted evidence-package tree remained unchanged, and the ledger still permits only `pg_restore --version` and `--list`. Final-head Linux/PostgreSQL 17, macOS, and PostgreSQL cross-major CI passed before merge; CI details remain live GitHub metadata. That repository work did not access the retained export or completed evidence package and did not access any runtime or external/pre-existing database. Source/archive completeness, full PGDMP-byte consumption, restore compatibility, and target readiness remain unproved. Migration readiness remains `RED`.

- **OFFLINE TOC CAPTURE ENVELOPE — PR #21 MERGED; ANNOTATION AND BOTH RESTORE GATES BLOCKED.** PR #21 merged with history preservation at main `6bdcb83e4006d04e8a8e6c54d1b6c243ec48adb1`; final reviewed head `cfbc5116b9dd7a4b5e502af67d7f4dd8d0c8e547` includes substantive startup-isolation head `219f78fba49d7b697037169c9436aa5aede1474c`. It adds a reviewed operator launcher around the existing strict ZIP-normalization/private opaque-TOC capture chain. The launcher requires one explicitly approved absolute execution-Python path plus its external SHA-256 and exact CPython version, validates the executable before and after an isolated probe, constructs a minimal child environment, and invokes the high-level driver only with `-I -S -B`. Every capture-owned Python child uses the same isolated/no-site/no-bytecode flags, and each internal component rejects unsupported direct execution before repository-local imports or sensitive processing. Pre-import Git checks bind the approved checkout and reviewed blobs while rejecting ordinary or ignored untracked migration-tool inputs and disabling hooks, fsmonitor, and inherited Git execution configuration. Fixed bounded diagnostics suppress raw child output and interpreter tracebacks, including broken or closed streams. Synthetic poison regressions prove user-site `.pth`, user `sitecustomize`, and `PYTHONPATH` startup code cannot read the planted private input, spawn an unexpected process, or leak a sentinel; the invocation ledger remains limited to bounded `pg_restore --version` and `--list`. The macOS synthetic lane selects `/usr/bin/python3` only after a silent exact regular-file, link-count, root-or-executor ownership, mode, executable, and isolation preflight; Linux retains the managed Python 3.12 lane. All canonical-artifact, normalization, cleanup, quarantine, no-replace publication, and restore-gate contracts remain in force. The retained export, completed evidence package, targets, runtimes, secrets, and external/pre-existing databases were not accessed by the repository work. The path-based interpreter launch still cannot attest the exact executable bytes consumed after the final pre-exec check, and the driver cannot prove source or archive completeness, full PGDMP-byte consumption, restore compatibility, or target readiness; migration readiness remains `RED`.

- **PRIVATE TOC CLASSIFICATION + POSTGRESQL 18 FIXTURES — PR #20 MERGED; RESTORE PLANNING BLOCKED.** PR #20 merged with history preservation at main `9ae5f98e44739c6a7a645e41527e6fa016d6fc63`. Its five-commit chain is `85821dd7c4b4613582d39e04daff80301277e64c` (initial substantive implementation), `653dbbc4213e6d86d342ee26f0a57276fa51dfdb` (intermediate STATE-only), `09c22a7cbd7c37f2b6f1a207cbe3d6243f90e2fa` (transport-hardening substantive), `b52fa791297f41162c020c30712072b8851a2b22` (prior final STATE-only head), and `359ad3c2a22912e87f1c0977eb094cbb8908daa7` (documentation-only final head). It adds a future separately authorized private raw-TOC capture and opaque human annotation ledger without relaxing the conservative aggregate parser or generating a restore command. The closed classifications are `restore`, `exclude_supabase_managed`, `exclude_duplicate`, `dependency_only`, `manual_conflict`, and `unresolved`; every TOC entry and data reference must be accounted for, relationship and managed-domain handling must be explicit, and any unresolved or incompletely disposed entry keeps the planning gate `BLOCKED`. Even full structural coverage can become only `ELIGIBLE_FOR_HUMAN_REVIEW`; `restore_command_gate` remains `BLOCKED` and migration readiness remains `RED`. Private raw TOC, key, index, annotation, and object identities are ignored and restricted to a future approved evidence store; public output is fixed/category-only with aggregate counts and hashes. A digest-pinned official-image CI lane uses disposable synthetic PostgreSQL 17.6 and 18.4 containers to prove only the exact fixture's dump/list/restore semantics, preserving PostgreSQL's lack of a guarantee for loading `pg_dump` 18 output into a PostgreSQL 17 server. The first PR-head-associated run reached the real offline list but exposed a Docker archive-copy portability failure; the final hardening streams the already bounded TOC privately, proves host/container byte-count equality, and preserves fixed nonleaking stage diagnostics. Final PR-head-associated GitHub Actions run `29615315284` succeeded before merge; CI details remain live GitHub metadata. This work did not access the retained export, durable evidence package, target, runtime, secrets, or any external/pre-existing database.

- **CRON ATTEMPT RECEIPTS — PR #19 MERGED; APPLICATION PROOF PARTIAL AND PRODUCTION BLOCKED.** PR #19 merged with history preservation at main `30d5a46c30a851d89957fcfba0cc0a5a53df24d2`; final reviewed head `63f930c23684c4c1b277d5149ed5dee1ab03bd90` contains substantive commit `16b05b6c2ef34d84edf14ae73f0289d7bf8a006a`, the primary hardening at `602f62a9748ae150fe5c29171fa17e2663e92486`, intermediate STATE-only head `474b51c0e9b025e8067cefd5e5793ec87036a7e9`, and automatic-grant correction `058462d93e3ec7bd628620e0beb2b007c951c66a`. Exact-head PR-event CI run `29569376375` succeeded before merge. Only the legacy receiver's leaf `handler.ts` and `index.ts` blobs are byte-identical to pre-PR main; shared `cronHeadReceiver.ts` and `cronSecretAuth.ts` changed, so no bundle/runtime identity is claimed. No tracked caller targets the strict `run-strategy-task-reaper-receipt-v1` slug; no deployment was authorized or observed; production deployment state is unavailable. Unscoped/deploy-all Edge Function deployment is prohibited, and only that strict slug may eventually be deployed under separate authorization after its gate clears. `strict_receiver_deployment_allowed()` is a deterministic synthetic/procedural model, not an installed runtime predicate or control-plane lock. Both exact-index guards reject `pg_index.indisexclusion`; isolated PG17 fixtures prove exclusion-index rejection, executor-role drift rejection, the non-superuser `CREATEROLE` automatic membership/owner-transfer contradiction, and receipt transaction behavior only in synthetic databases. The receipt SQL remains outside ordinary migrations and requires a separately authorized exact index plus externally pre-provisioned executor. The final PR scope was 29 files. `daily-digest` and `schedule-daily-plan` remain RED; `APPLICATION_RECEIPT_STATUS` remains `PARTIAL_BLOCKED`; controlled SQL, receipt installation/index/pruning, receiver deployment, sender activation, controlled dispatch, handoff mutation, and production rotation all remain BLOCKED. No real credential, function, deployment, invocation, rotation, index, role, Lovable/Supabase/n8n runtime, external or pre-existing database, staging, production, secret store, or retained export was accessed or changed.
<!-- Superseded mutable CURRENT STATE wording retained only as hidden source
context. The PR #19 entry above is authoritative.
- **CRON ATTEMPT RECEIPTS — PR #19 OPEN/DRAFT/UNMERGED; APPLICATION PROOF PARTIAL AND PRODUCTION BLOCKED.** PR #18 merged before verified main `0275d879148c3299d54846333d304c2ac8476b64`. Draft PR #19 at latest substantive head `7d5f75d1a39de0789423c43c67d3a187b5c19ea8` implements one exact durable `x-cron-attempt-id` contract only for the cron-authenticated `run-strategy-task-reaper` path. Its fixed PostgreSQL wrapper serializes the bound attempt, deterministically locks and transitions the eligible `task_runs` set, and commits the terminal receipt in one transaction; exact/concurrent retries return the same durable result, identity conflicts fail closed, a planted terminal-write failure leaves neither effect nor receipt, proven rollback has a fixed failure outcome, and no eligible rows has a distinct legitimate-no-op receipt. Two distinct attempts racing for the same row yield exactly one transition, one applied receipt, and one legitimate-no-op receipt. Authenticated `HEAD` remains side-effect free, and the existing `daily-digest` user-JWT path remains separate. The attempt is rejected before hashing/client/RPC work if it equals either configured receiver slot or another protected request/runtime credential; the credentials are never hashed or persisted, and authentication, project binding, and actual RPC client construction share one request-local environment snapshot. The wrapper owner is a dedicated non-superuser/NOLOGIN role with exact task column, private-state, RLS-policy, capability-flag, and zero-membership boundaries; planted missing/broader privileges, role drift, PUBLIC/executor policy drift, and late migration failure all fail closed in PostgreSQL 17. Routine migration no longer builds the task-run index: an independently authorized PostgreSQL-17 concurrent template or explicit maintenance window must satisfy the exact index prerequisite first. Production's active legacy no-attempt callers prohibit strict receiver deployment until pause/drain, receipt migration, and inactive attempt-capable sender preparation are complete. Protocol-v1 receipts remain indefinitely retained while v1 is accepted; no pruning RPC, privilege, policy, or schedule is installed. `daily-digest` remains RED because model calls and separately committed digest/account effects cross the receipt transaction boundary; `schedule-daily-plan` remains RED because its per-user downstream HTTP/model/database effects cross the parent boundary. Therefore `APPLICATION_RECEIPT_STATUS` is only `PARTIAL_BLOCKED`; `CONTROLLED_SQL_VERIFICATION_STATUS` remains `TEMPLATE_ONLY_BLOCKED`, and `RECEIVER_DEPLOYMENT_GATE`, `RECEIPT_INDEX_DEPLOYMENT_GATE`, `RECEIPT_PRUNING_GATE`, `SENDER_ACTIVATION_GATE`, `CONTROLLED_DISPATCH_GATE`, `LEGACY_HANDOFF_MUTATION_GATE`, and `PRODUCTION_ROTATION_GATE` remain `BLOCKED`. Local repository, application, and isolated PostgreSQL 17 checks are green; Deno and the complete high-level inspection integration remain exact-final-head CI gates. The fixture is a compatible minimal repository-known shape, not proof against live owner/ACL/RLS/trigger/index drift, managed migration-actor capability, control-plane target identity, or deployed configuration. Both migration-evidence fixtures deliberately reconstruct the exact historical migration baseline while using current reviewed tools, so later application migrations cannot silently redefine that evidence boundary; production baseline enforcement is unchanged. This work used repository code, synthetic fixtures, and isolated local PostgreSQL 17 databases only; it did not access or change a real credential, function, deployment, rotation, Lovable/Supabase/n8n runtime, external or pre-existing database, staging, production, secret store, or retained export.
-->
<!-- Superseded mutable CURRENT STATE wording retained only as hidden source
context. The PR #19 entry above is authoritative for receipt status.
- **CRON-CREDENTIAL ROTATION READINESS — PR #18 HISTORICAL MERGED DESIGN BASELINE; SUPERSEDED BY THE PR #19 RECEIPT STATUS ABOVE.** Draft PR #18 at latest substantive head `6801637f8ffe7e4b38ece263aa63237f161bf17b` retains independent current/next receiver slots and rejects reuse among the accepted cron secret, rejected control, gateway API key, and optional gateway JWT before any harness fetch or template dispatch. The exported verifier now independently requires primitive runtime strings and validates the environment/phase/slot enums before URL parsing or fetch, so type erasure cannot make numbers, boxed strings, arrays, objects, or other coercible values into credential or routing inputs. Header credentials require one canonical visible-ASCII representation; the API key is sent only as `apikey`, a separately supplied JWT is sent only as `Authorization`, and reviewed `verify_jwt=false` probes omit Authorization. CI binds the harness's `reviewed_expected_verify_jwt` map to `supabase/config.toml` plus `docs/migration/edge-functions.json`; this is reviewed repository expectation, not observed runtime configuration. The Phase 3.6 application test is explicitly a lexical source-binding drift guard and does not prove import/call reachability or deployed code. Corey-supplied sanitized production evidence reports active legacy jobs `7`, `9`, and `15` as Edge Function callers with plaintext cron-header construction and no Vault use; this PR did not observe production. The future handoff preserves each job's non-secret name/schedule/owner/database/function-slug mapping through one inactive fixed Vault wrapper per tuple, forbids duplicate schedules and plaintext rollback, and now requires `RECEIVER_PROMOTED_PREDECESSOR_REJECTED` before one all-or-none, cron-mutation-fenced final-enable transaction. Its synthetic model revalidates exact identities, uniqueness, legacy-ID absence, wrapper fingerprints, plaintext absence, receiver-promotion evidence, and inactive/active postconditions before and after activation; every planted failure rolls back to all replacements inactive. This remains a future repository contract, not installed or runtime proof. The repository implements no exact durable attempt-bound application receipt for success, failure, duplicate, or legitimate no-op outcomes. `APPLICATION_RECEIPT_STATUS` remains `NOT_IMPLEMENTED`; controlled SQL is template-only; sender activation, controlled dispatch, legacy-handoff mutation, and production rotation are `BLOCKED`. Production actors must stop before `LEGACY_PAUSED`; no pause, wrapper invocation, unschedule, replacement, promotion, or enablement is ready or authorized. Prior read-only discovery established metadata and SQL-read access to user-owned dynamic-staging, reported `pg_cron`, `pg_net`, and `supabase_vault` installed, and returned zero cron jobs; that observation alone does not prove there is no external caller and must not cause a scheduler to be installed. Production `odbjjklumdsuqdvkgwyv` remained permission denied. Corey remains the staging rotation actor; Corey plus Lovable Support/admin remain the production actors. This repository-only work did not rotate or access a credential, invoke a function, deploy, or access/mutate any Lovable, Supabase, or n8n project/runtime, production or staging system, secret store, pre-existing database, or retained export. Both incidents and all evidence remain separate; both external rotations remain open.
-->
- **DYNAMIC-STAGING CREDENTIAL CONTAINMENT — PR #17 MERGED; EXTERNAL ROTATION BLOCKER OPEN.** PR #17 merged with history preservation at main `b6cb6816b3f2501a8436b36f1b20aedc2798018f`; its latest substantive head was `57c34cd283c05df8085724a7cea379bd059938a7`. It removes credential-shaped literals and the executable schedule block from the current derived `supabase/dynamic_staging_schema.sql` snapshot. That snapshot is non-executable repository drift evidence outside chronological migrations; no runtime schedule was disabled, removed, recreated, or changed, and runtime state remains unverified. Historical repository evidence recorded nine snapshot schedules total: migration-defined `ops_sentinel_v1` plus eight additional schedules. The sanitized snapshot has no reviewed executable-cron, staging/production project-binding, cron-header, or bearer-JWT patterns. The scanner is a known-pattern accidental-regression guard over every tracked `.sql` file and only tracked `.ini`, `.json`, `.toml`, `.yaml`, and `.yml` files under `supabase/`; it is not an exhaustive SQL/configuration security boundary. Except for the one reviewed derived-snapshot path, diagnostics replace filenames with an opaque artifact label. Prior independent repository verification—not this category-only scanner—classified the removed bearer material as anon/publishable client material rather than service-role authority; it still must not be hardcoded into cron SQL. The public PR patch disclosed predecessor bytes despite the removed diff-rendering hint, so the PR patch, Git history, forks, caches, archived patches, and prior clones are persistent exposure surfaces. External rotation of the staging cron secret is the required open mitigation; this repository containment did not rotate or verify it, and history rewriting remains out of scope. Recreating jobs initially disabled is operator policy, not a repository-enforced state. CI validation used only isolated synthetic PostgreSQL fixtures.
- **MIGRATION INSPECTION TOOLING — PR #16 MERGED; REVIEW_REQUIRED / READINESS RED.** PR #16 merged with history preservation at main `1b13483be43b3d5f28a7086606b8a921a6879f18`. Its aggregate-only incomplete-analysis and runtime-bound publication controls remain tooling evidence only. A separately authorized offline attempt subsequently produced an operator-reported complete durable metadata package and performed no restore. Its sanitized aggregate is 2,354 TOC entries, 2,140 metadata entries, 214 data references, and 1,135 recognized-but-unresolved entries, with source PostgreSQL 17.6, writer `pg_dump` 18.4, and inspection client `pg_restore` 17.10. Status remains `REVIEW_REQUIRED`, restore planning remains `BLOCKED`, and the consumed attempt authorizes no further artifact access. This repository work did not access or independently re-establish the export or package. The tooling cannot prove Lovable source completeness or UI-to-backend mapping, independently attest operator/UI claims, prove every inner byte was consumed, validate a restore, or establish target readiness.

#### Historical detail (Jul 14; superseded where Jul 15 entries conflict)

- **SUPABASE MIGRATION REHEARSAL — RUNTIME-BOUND EVIDENCE CONTRACT IN DRAFT; READINESS RED.** PR #13, ZIP-envelope PR #14, and diagnostic PR #15 are merged with history-preserving merge commits; verified main remains `8b872882787859b87549e7f884832c624e29ead9`. One authorized rehearsal export remains retained offline in the approved evidence store. A separately authorized metadata-only attempt was reported to have failed closed at `report_helper_failed` / `unresolved_known_toc_entry`; it produced no evidence package and performed no restore. No identifying artifact path, name, size, hash, or timestamp is recorded here. Draft PR #16 at latest substantive head `db8929215bd0134038372f9072e26a829725df84` retains the aggregate-only correction and now freezes one runtime publication expectation before retained sidecars/provenance are constructed. That immutable binding contains the exact live canonical descriptor identity plus approved checkout/tool/config/Python, source, timeline, normalizer envelope/member, inner/header, report, and analysis identities; pending, descriptor-staged, immediate pre-rename, and post-rename pre-completion gates all revalidate provenance and the live canonical descriptor. Fully rehashed coherent outer/sidecar/manifest, checkout/tool/config/project, envelope, and report/analysis substitutions can satisfy internal structural validation but cannot publish against runtime truth; a matching positive package traverses every gate. Provenance format version 6 permits only `migration_duplicate_analysis=CONSERVATIVE` for resolved-object name heuristics or `INCOMPLETE` for unresolved aggregate evidence; this scan never emits `COMPLETE`, cannot prove the absence of duplicates across PostgreSQL aliases, modifiers, object-kind ambiguity, or dynamic SQL, and always retains `restore_planning_gate=BLOCKED`. Local repository-only validation is green (162 migration Python tests, 113 raw-inspector checks, 26 target-safety regressions, eight shell syntax checks, seven Python no-write compilations, six decisive runtime-binding/SQL-form regressions, and diff checks). Initial final-head run `29429575704` passed the macOS publication job and all Linux gates through the PostgreSQL catalog/service/raw-dump integrations, then exposed one stale high-level integration assertion that still required provenance format version 5; substantive commit `db8929215bd0134038372f9072e26a829725df84` now requires version 6 without changing the production contract. Corrected final-head Linux/PostgreSQL 17 application/Deno/audit/integration checks and macOS publication checks remain required in CI. Evidence completion still cannot mean restore readiness or migration green. The tooling cannot prove Lovable source completeness or UI-to-backend mapping, independently attest operator/UI claims, prove every inner byte was consumed, validate a restore, or establish target readiness. PR #16 did not access, stat, hash, read, copy, rename, chmod, unzip, normalize, or inspect the retained export and did not access Lovable, Supabase, production, secrets, or any external/pre-existing database; it performed no restore, deployment, migration, export, or data movement. `.lovable/plan.md` remains rejected historical input.

#### New (Jul 12)

- **PRODUCTION `version` RESPONSE — JULY 12 OBSERVED, LIMITED PROOF.** PR #11 merged as `447c00f6bdfe988053e8e9ee650a834679a1a7fc`. A July 12 observation of the public production `version` function returned release `edge-20260712-5e071bee2975`, source `5e071bee29751f549dc3ae3f5308e0d81005be72`, and a project-scoped deployment ID. This proves only what that deployed `version` bundle returned at that observation time. It does not independently attest `strategy-chat`, `analyze-call`, or `mcp`, prove that the named source commit produced every deployed bundle, or establish current production state; tracked release metadata can name a commit other than the bundle-producing commit.
- **ALL-FUNCTION RUNTIME ATTESTATION — OPEN DRAFT, NOT DEPLOYED.** PR #12 is open and draft at `a70ea4e1e3cdaa588546cbd3730481ae85b664cd`. It proposes coordinated build/deploy/verification for `strategy-chat`, `analyze-call`, `mcp`, and `version`, but remains blocked on a protected `production` environment with `SUPABASE_ACCESS_TOKEN`, protected `main`, and removal/restriction of out-of-band Supabase deploy authority. Merge and the coordinated four-function redeploy both require separate authorization; neither occurred here.

#### New (Jul 11 night)

- **S-R1 — RETRIEVAL RELEVANCE/RANKING.** The Sources panel exposed low-relevance items entering evidence (generic sales KIs on a competitive turn), creating budget pressure and mis-citation risk. Investigate `situationIntelligenceRetrieval` scoring. P1, post-Monday.
- **S-R2 — SOURCES PANEL → FULL VERIFICATION VIEW.** Add clickable provenance and card-versus-playbook distinction. P2.
- **S-R3 — PROMPT BUDGET RECKONING.** 17,491/17,500 leaves approximately zero headroom. Additions need a paired trim or a deliberate ceiling raise plus revalidation. Linked to S-R1. P1.

#### Carried from v20

- **L3 PRODUCT wiring:** Unscoped. Day 1: upload Branch onboarding docs → Library → Product Intelligence head.
- **SUPABASE MIGRATION to own account:** PRs #13 through #23 are merged. Repository state records an operator-reported completed offline metadata inspection and a separately authorized completed private raw-TOC capture; this PR does not re-access or independently reverify either package. Private annotation authoring, ledger validation, restore planning, and every runtime step remain separately gated and unperformed. No restore, target project creation/access, remix, or cutover has occurred. Production remains on Lovable-managed `odbjjklumdsuqdvkgwyv`. Keep the original project intact and any future target read-only until rollback-critical verification passes.
- **`account_risks` population gap:** Only 2/9 fluency accounts yield a competitive signal; the debrief-capture pipeline is the real fix.
- **`competitive_intel` freshness:** Spot-verify AppsFlyer (Jun 2026 raise) and Kochava (2026 FTC settlement) card text against live rows.
- **Web Research widening:** Reconcile onto new `main`, then rerun CI and the prompt budget before merge.

### §E. STANDING / KNOWN SIGNATURES

Deployment has three parts and can split-brain: a GitHub merge does not make code live. Edge functions require `supabase functions deploy`; the frontend requires Lovable Publish → Update; deploy them together. The deploy build runs strict full-project `tsc`, including the frontend, so a pre-existing frontend error can block an edge deploy—the root cause of the Jul 11 “Build unsuccessful” result.

Trust-but-verify held throughout the session: it caught an unmerged PR before a bad deploy, a CARD drop that a green suite missed, and a fabricated citation that the guard now flags. Branch plus SHA is the only trusted delivery. No merge without explicit human approval. Day 1 is July 13.

---

**END v21.** v20 recorded the corpus scale-up; v21 records the night Strategy's trust layer went live: citations that cannot fabricate, competitive intel that reaches the model, and identity grounded in real territory data. All verified live in production Jul 11. Day 1 is July 13. Go drive it.

# SESSION LOG

> Append-only, newest first. Add each new immutable entry directly below this note; never rewrite prior entries.

## 2026-07-19 — Codex — Annotation invocation tuples rejected before private access

- **Who:** Codex, at Corey Hartin's direction.
- **What:** Opened draft PR #24 from freshly verified main `1b0116a09d05a9f787c27d9a24a2f021fcecca0f` using repository code and synthetic fixtures only. A consolidated side-effect-free validator now enforces static initialization, resume, operator-role, finalization-authorization, and predecessor-checkout tuples before execution-tool validation, any private/capture path access, durable lock or directory mutation, capture read, or checkpoint load. Existing later package/checkpoint identity and history checks remain. Table-driven invalid tuples make zero private-operation calls, leave synthetic roots and capture metadata unchanged, create no lock/checkpoint/marker/final package, and emit only fixed nonleaking diagnostics; the exact valid initialization tuple still reaches the synthetic path, while the isolated controlling-TTY regression rejects invalid initialization before planted roots are touched. Local annotation/startup validation passed 78 tests with two expected sandbox-only process-ancestry skips; substantive-head CI run `29688934743` passed all three Linux/PostgreSQL 17, macOS, and cross-major jobs. Initialization remains unrun and blocked pending merge, fresh merged-main preflight, and separate authorization. Validation, restore planning, restore commands, and migration readiness remain `BLOCKED`/`RED`. No private artifact, annotation root, export, Lovable, Supabase, n8n, external/pre-existing database, target, secret, deployment, or runtime was accessed or changed.
- **SHAs:** substantive draft PR #24 head `e13c5acdaf13e80bd7fe31a3eaa59fff55d21684`; verified starting main `1b0116a09d05a9f787c27d9a24a2f021fcecca0f`; merged PR #23 reviewed head `59b10829b2bf9c915e8e64bb13b6ab70fe484940`.

## 2026-07-18 — Codex — Manual-conflict correction and status cleanup hardened

- **Who:** Codex, at Corey Hartin's direction.
- **What:** Hardened open draft PR #23 using repository code and synthetic fixtures only. A scoped primary-classification review may now clear an existing `manual_conflict_disposition` only to null. The resulting decision must remain explicitly classification-reviewed; selecting `manual_conflict` requires a fresh dedicated disposition review, while every other class makes that phase not applicable. Retaining or replacing an old final disposition or preserving stale reviewed state fails closed. Corrections from both pre-publication finalization states and peer-requested re-review recompute the canonical decision hash, invalidate peer approval, and preserve ordinary phase order; only `manual_conflict_review` may choose the final disposition. A descriptor-close/cleanup ambiguity during nominal aggregate-only `status` now overrides exit 2 with fixed `cleanup_indeterminate`, leaves a blocking lock/indeterminate state, and withholds normal resume values and durable release. Local validation passed 60 authoring/checkpoint tests, 15 startup/TTY tests with one expected sandbox skip, the complete 373-test migration Python suite with two expected local skips, 70 repository security tests plus a zero-output current-tree scan, 113 inspector checks, 22 dump-report checks, 26 migration target-safety checks, three cron target-safety checks, Python/schema syntax, and base-range diff checks. Node/npm and Deno are unavailable locally, so application, typecheck, Deno, audit, PostgreSQL integration, macOS behavior, and final base-range checks remain exact-final-head CI evidence. PR #23 remains open/draft/unmerged; authoring and validation were not run against private evidence, a generated ledger remains `REVIEW_REQUIRED`, and validation, restore planning, restore commands, and migration readiness remain `BLOCKED`/`RED`. No real capture/evidence package, canonical export, Lovable, Supabase, n8n, external/pre-existing database, target, credential, secret, deployment, or runtime was accessed or changed.
- **SHAs:** latest substantive draft PR #23 head `4d6c2a61257528a36f7a01cad1d12ddad7167bcf`; prior final head `999a6f39e0568455259124180d6dcb78555c801a`; verified starting main `c78a0e09373235fe38d045c2bfa7a0fd8a32d364`.

## 2026-07-18 — Codex — Pre-publication annotation correction hardened

- **Who:** Codex, at Corey Hartin's direction.
- **What:** Hardened open draft PR #23 using repository code and synthetic fixtures only. Before final-candidate publication, the explicit `correction_review` route now operates from either `FINALIZATION_REVIEW_REQUIRED` or `FINALIZATION_ELIGIBLE`; eligibility remains a mechanical state rather than a claim of semantic infallibility or restore readiness. A distinct internal relationship-correction event re-prompts exact private-TTY `dependency` and applicable `structural_parent` roles after completed review, permits clear/replace/reselect, recomputes the canonical role-keyed decision hash, and invalidates affected peer approval without permitting unrelated fields or an ordinary phase-order bypass. Clearing a required parent returns relationship review to pending. Any `SEQUENCE OWNED BY` structural-parent change must also reset sequence review to pending, after which fresh `sequence_structural_parent` context and acknowledgement and fresh peer approval are required; a forged stale sequence confirmation fails closed. `SEQUENCE SET` remains confined to `sequence_metadata_parent`, and ordinary data-reference correction remains confined to `metadata_parent`. Final-package no-replace publication remains terminal. Local validation passed 52 authoring/checkpoint tests, 15 startup/TTY tests with one expected sandbox skip, the complete 365-test migration Python suite with two expected local skips, 70 repository security tests plus a zero-output current-tree scan, 113 inspector checks, 22 dump-report checks, 26 migration target-safety checks, three cron target-safety checks, Python/schema/shell syntax, and diff checks. Node and Deno are unavailable locally, so application, typecheck, Deno, audit, PostgreSQL integration, macOS behavior, and final base-range checks remain exact-final-head CI evidence. PR #23 remains open/draft/unmerged; authoring and validation were not run against private evidence, a generated ledger remains `REVIEW_REQUIRED`, and validation, restore planning, restore commands, and migration readiness remain `BLOCKED`/`RED`. No real capture/evidence package, canonical export, Lovable, Supabase, n8n, external/pre-existing database, target, credential, secret, deployment, or runtime was accessed or changed.
- **SHAs:** latest substantive draft PR #23 head `bc2b432e70e48d318fb1c8e80b86505298d90dac`; prior final head `b4c1151b40936af3343cde23d93f3f8987033013`; verified starting main `c78a0e09373235fe38d045c2bfa7a0fd8a32d364`.

## 2026-07-18 — Codex — Annotation startup poison control made runner-independent

- **Who:** Codex, at Corey Hartin's direction.
- **What:** PR-head-associated GitHub Actions run `29664272257` passed the macOS annotation/publication job and PostgreSQL 17.6/18.4 cross-major job. Linux passed repository security, dependency installation, typecheck, and application tests, then its duplicate annotation startup suite exposed only that the positive control assumed the managed runner automatically loaded a planted user-site `sitecustomize` module. The production launcher had already rejected both planted startup variables before its reviewed child. The focused correction now explicitly loads the exact planted `.pth`, user-site `sitecustomize`, and `PYTHONPATH` modules outside isolation to prove each payload is viable on every runner; the actual launcher regression performs none of those explicit loads and retains its minimal `env -i` plus `-I -S -B` boundary. The corrected 15-test startup/TTY suite passes locally. No production check, authoring state transition, diagnostic, checkpoint, finalization, or validator contract was relaxed. Exact-final-head CI remains live GitHub metadata. Every annotation-validation, restore-planning, restore-command, target, and migration gate remains BLOCKED/RED, and no real capture/evidence package or external system was accessed.
- **SHAs:** latest substantive draft PR #23 head `5954a9463ad60a704c4c201070778095465f83a0`; prior final STATE-only head `fde72e0d5155294642c7af93aa119261308f841c`; startup-control head `e47c1c37c9ea5ee22f9c9fc410bf3462078b52dd`; authoring implementation `c3d99a7c5b4cfb1b34801d1b41667e51962eafca`; verified starting main `c78a0e09373235fe38d045c2bfa7a0fd8a32d364`.

## 2026-07-18 — Codex — Private TOC role review and resume handoff hardened

- **Who:** Codex, at Corey Hartin's direction.
- **What:** Hardened open draft PR #23 at substantive head `5954a9463ad60a704c4c201070778095465f83a0` without accessing private evidence or any runtime. Relationship selection and peer review now preserve fixed `dependency`, `structural_parent`, `metadata_parent`, `sequence_metadata_parent`, and `sequence_structural_parent` labels. Peer review acknowledges the primary-decision summary before the first clear and then acknowledges each role page; multi-parent sequence review acknowledges each parent separately. Swapped assignments with identical counts/reference unions produce distinct private transcripts and decision hashes, while multi-role entries display once per role and opaque IDs/sentinels remain absent from ordinary output. The exact resume tuple is now displayed and acknowledged before durable lock release. TTY write, EOF, wrong acknowledgement, terminal attribute/read/restore, and ordinary prompt-restore failures leave `AUTHORING_LOCK` plus best-effort `AUTHORING_INDETERMINATE`, never a normal released state. The documented ceiling now states that a filesystem rejecting both restoration paths after a release durability error is an ambiguous no-retry stop. Local validation passed 48 authoring/checkpoint tests, 15 startup/TTY tests with one expected sandbox skip, the complete 361-test migration Python suite with two expected local skips, 70 repository security tests plus a zero-output current-tree scan, 113 inspector checks, 22 dump-report checks, 26 migration target-safety checks, three cron target-safety checks, shell/Python/schema syntax, and diff checks. Node/npm and Deno are unavailable locally, so application/typecheck/Deno/audit plus Linux PostgreSQL integration and macOS behavior remain exact-final-head CI gates. PR #23 remains open/draft/unmerged; a generated ledger remains `REVIEW_REQUIRED`, and validation, restore planning, restore commands, and migration readiness remain `BLOCKED`/`RED`. No real capture/evidence package, canonical export, Lovable, Supabase, n8n, external/pre-existing database, target, credential, secret, deployment, or runtime was accessed or changed.
- **SHAs:** latest substantive draft PR #23 head `5954a9463ad60a704c4c201070778095465f83a0`; prior final head `fde72e0d5155294642c7af93aa119261308f841c`; verified starting main `c78a0e09373235fe38d045c2bfa7a0fd8a32d364`.

## 2026-07-18 — Codex — Private TOC annotation authoring drafted

- **Who:** Codex, at Corey Hartin's direction.
- **What:** Opened draft PR #23 from freshly verified main `c78a0e09373235fe38d045c2bfa7a0fd8a32d364` using repository code and synthetic fixtures only. The authoring workflow now requires one reviewed zero-argument launcher, a minimal `env -i` child, `-I -S -B`, exact interpreter/check-out/tool identities, a verified local foreground controlling TTY, and a clean tracked migration-tool closure before private input access. It opens no opaque key bytes and emits no raw context outside its bounded escaped TTY surface. Separate immutable checkpoints record explicit primary decisions, all relationship/data/sequence/managed/manual-conflict phases, and distinct peer review; one durable private release token plus exact generation/hash is required to resume. Stale locks, forked histories, malformed or missing generations, concurrency, publication ambiguity, and incomplete semantic review stop fail-closed. Finalization is a separate explicit action that can publish only an unvalidated private ledger candidate; authoring does not call the validator. A second isolated zero-argument launcher preserves the existing validator's 15-variable contract for a future separate authorization. Local synthetic validation passed 44 authoring/checkpoint, 15 startup/TTY, 41 TOC-contract, 70 security, 26 migration target-safety, and three cron target-safety tests; the complete migration Python suite ran 357 tests with 355 passing and two expected local skips that remain exact-head CI gates. Exact-head CI details remain live GitHub metadata. Unknown terminal recording/screen capture and a hostile same-user environment remain local trust ceilings. Every annotation-validation, restore-planning, restore-command, target, and migration gate remains BLOCKED/RED. No real capture/evidence package, export, Lovable, Supabase, n8n, database, target, secret, deployment, or runtime was accessed or changed.
- **SHAs:** draft PR #23 substantive head `c3d99a7c5b4cfb1b34801d1b41667e51962eafca`; verified starting main `c78a0e09373235fe38d045c2bfa7a0fd8a32d364`.

## 2026-07-18 — Codex — Real PostgreSQL inspector ledger fixture corrected

- **Who:** Codex, at Corey Hartin's direction.
- **What:** PR-head-associated GitHub Actions run `29653909984` passed macOS, PostgreSQL 17.6/18.4 cross-major, repository security, typecheck, application tests, all 298 migration Python tests, and the new real PostgreSQL 17 bounded `pg_restore --list` regression. The later standalone real-archive integration then stopped before its inspector command because the test script declared its local ledger pathname readonly and tried to export that same shell variable name. The fixture now keeps the readonly pathname under a distinct local name and exports only the child ledger variable. This changes no production wrapper, capture, cleanup, quarantine, publication, or runtime contract. Shell syntax and diff checks pass; the corrected real integration and all exact-head jobs remain CI gates. Every capture, annotation, restore, and migration gate remains BLOCKED/RED. No retained export/evidence package, Lovable, Supabase, n8n, external/pre-existing database, target, secret, deployment, or runtime was accessed.
- **SHAs:** latest substantive draft PR #22 head `ccf289b1451ee797ba391b8a0a4f398c374bea1d`; wrapper correction `9fda6565a49712bae89effd848c31be73e8fb379`; prior STATE head `63c280d1b831bf495b3f1c7862ac3173de22a260`; verified starting main `6bdcb83e4006d04e8a8e6c54d1b6c243ec48adb1`.

## 2026-07-18 — Codex — Bounded pg_restore captures confined to private staging

- **Who:** Codex, at Corey Hartin's direction.
- **What:** Opened draft PR #22 from freshly verified main `6bdcb83e4006d04e8a8e6c54d1b6c243ec48adb1` to correct the merged bounded-wrapper cleanup boundary without running the retained-export workflow. The wrapper no longer defaults successful `pg_restore` output captures to ambient system temporary storage: it requires one explicit validated private parent, creates captures descriptor-relatively, withholds successful bytes until unlink/removal and parent fsync succeed, and converts reap, close, unlink, or fsync ambiguity into fixed failure plus private indeterminate handling where state survives. The high-level envelope binds that parent to its held staging descriptor, while the standalone inspector binds it to its own held private work-directory descriptor and suppresses descriptor-open/close path errors. Synthetic PostgreSQL-success-plus-cleanup-failure plants prove raw TOC capture preceded failure, no top-level system-temp wrapper capture appeared, no complete package published, and surviving raw state is either removed or held only in private quarantine with the reviewed marker. They also prove nonleaking diagnostics, exact version/list invocation, broader canonical identity preservation, and nonmutation of a complete planted evidence-package tree. Local checks passed 298 migration tests, 113 inspector checks, 70 security tests and the current-tree scan, 26 target-safety checks, three cron target-safety checks, syntax, and diff checks. The real PostgreSQL 17 list path and Linux/macOS/cross-major acceptance remain exact-final-head CI gates. The optional launcher-local fixed-diagnostic temp cleanup failure was not planted because it contains only bounded allowlisted diagnostics rather than raw TOC bytes; it remains an explicit defense-in-depth ceiling. Every capture, annotation, restore-planning, restore-command, and migration-readiness gate remains BLOCKED/RED. No retained export/evidence package, Lovable, Supabase, n8n, external/pre-existing database, target, secret, deployment, or runtime was accessed.
- **SHAs:** draft PR #22 substantive head `9fda6565a49712bae89effd848c31be73e8fb379`; verified starting main `6bdcb83e4006d04e8a8e6c54d1b6c243ec48adb1`.

## 2026-07-18 — Codex — TOC startup regressions made runner-independent

- **Who:** Codex, at Corey Hartin's direction.
- **What:** Follow-up exact-head run `29649315677` proved the revised interpreter selection reached the macOS migration suites and the PostgreSQL 17.6/18.4 lane passed, then exposed two remaining synthetic expectation assumptions. The global effective-UID-drift regression can encounter either the execution-interpreter binding or a private-root binding first depending on whether the selected platform interpreter is root-owned or executor-owned; it now accepts only those two reviewed fixed fail-closed reasons while still requiring zero child calls, no publication, full disposable cleanup, and canonical-input immutability. The separate execution-Python wrong-owner regression continues to require exact `binding_mismatch`. The user-site positive control now directly executes the exact planted `.pth` import line and imports each planted `sitecustomize` module from its intended directory, proving the payloads are viable without relying on CI-specific automatic-site policy; the actual launcher path does none of those explicit imports and remains the decisive `-I -S -B` nonexecution proof. No production check or allowlist was relaxed. Local focused checks passed 42 driver tests and eight startup-isolation tests; the previously completed 290-test migration suite and all other local validation remain applicable because only these two regression expectations changed. Exact-final-head CI remains live GitHub metadata. All annotation and restore gates remain `BLOCKED`, migration readiness remains `RED`, and no capture retry is authorized. No retained export or completed evidence package was accessed, and no Lovable, Supabase, n8n, external/pre-existing database, target, secret, deployment, or runtime was accessed or changed.
- **SHAs:** runner-independent substantive draft PR #21 head `219f78fba49d7b697037169c9436aa5aede1474c`; platform-CI substantive head `87dbcda0e294be14b738fe20290f454f67cf131f`; prior STATE-only head `88bc23a171a01c49b5c80d91b369c13b074579a6`; verified starting main `9ae5f98e44739c6a7a645e41527e6fa016d6fc63`.

## 2026-07-18 — Codex — TOC startup CI platform boundary corrected

- **Who:** Codex, at Corey Hartin's direction.
- **What:** Exact-head PR #21 GitHub Actions run `29648719093` exposed two synthetic test-boundary failures while the production checks failed closed. On macOS, the setup-python framework interpreter reached but did not satisfy the reviewed final owner/mode safety predicate; the job did not print its exact mode, so no more specific runtime fact is claimed. The macOS publication lane now avoids that managed interpreter, uses `/usr/bin/python3` through a restricted `/usr/bin:/bin` path, and first performs a silent fixed ownership, mode, link-count, executable, and isolated-runtime preflight. Linux continues to provide managed CPython 3.12 coverage. On Linux, setup-python disabled automatic user-site loading, so the planted `.pth` and user `sitecustomize` positive control did not run. The portable control now explicitly adds the planted user-site directory and imports `sitecustomize`, proving both payloads are viable without relying on runner-specific user-site policy; the actual capture-chain regression still depends only on `-I -S -B` and does not perform that load. A static regression binds the macOS job to the reviewed system-interpreter contract. The production interpreter checks were not relaxed and no host interpreter was chmodded or otherwise changed. The revised complete local migration suite passed 290 tests, including eight startup-isolation tests, and diff/shell checks passed. Exact-final-head CI remains live GitHub metadata. All annotation and restore gates remain `BLOCKED`, migration readiness remains `RED`, and no capture retry is authorized. No retained export or completed evidence package was accessed, and no Lovable, Supabase, n8n, external/pre-existing database, target, secret, deployment, or runtime was accessed or changed.
- **SHAs:** CI-platform substantive draft PR #21 head `87dbcda0e294be14b738fe20290f454f67cf131f`; startup-isolation substantive head `d085f8ed66b420c8f79e47fcec43533a429b7d59`; prior STATE-only head `02a768e984e28af8297a8ce3ba8310e24eacc163`; verified starting main `9ae5f98e44739c6a7a645e41527e6fa016d6fc63`.

## 2026-07-18 — Codex — TOC capture Python startup isolated

- **Who:** Codex, at Corey Hartin's direction.
- **What:** Hardened existing open draft PR #21 using repository code and synthetic fixtures only. A reviewed shell launcher now requires an explicit absolute execution-Python path, external SHA-256, and exact CPython version; verifies its ownership, permissions, canonical path, digest before and after an isolated version probe; constructs a minimal environment; and invokes the high-level capture driver with `-I -S -B`. Every capture-owned Python child now uses those same flags, and each internal Python component requires isolated, ignored-environment, no-user-site, no-site, and no-bytecode runtime state before repository-local imports or sensitive processing. Before imports, hardened Git calls bind the approved checkout and reviewed files, disable inherited hooks/fsmonitor/configuration, and reject ordinary or ignored untracked migration-tool inputs. Both capture entrypoints use bounded exception-safe fixed-diagnostic writes, while the launcher privately bounds and validates child output before releasing one allowlisted diagnostic. Planted user-site `.pth`, user `sitecustomize`, and `PYTHONPATH` poison controls execute outside isolation but cannot read a synthetic mode-`0400` private input, spawn a marker process, or leak sentinels through the reviewed chain; the subprocess ledger still permits only bounded `pg_restore --version` and `--list`. Closed-pipe, broken-stream, raw/oversized child output, interpreter replacement, repository-shadow, and Git hook/fsmonitor regressions fail safely. Local validation passed 289 migration tests, 70 security tests, 113 inspector checks, 26 target-safety checks, three cron target-safety checks, 12 cross-major contract tests, shell/Python syntax, the current-tree scanner, and diff checks. Exact-final-head Linux, macOS, and PostgreSQL cross-major CI remains live GitHub metadata. The path-based interpreter launch cannot attest the exact executable bytes consumed after its final pre-exec check. All annotation and restore gates remain `BLOCKED`, migration readiness remains `RED`, and this work authorizes no capture retry. No retained export or completed evidence package was accessed, and no Lovable, Supabase, n8n, external/pre-existing database, target, secret, deployment, or runtime was accessed or changed.
- **SHAs:** latest substantive draft PR #21 head `d085f8ed66b420c8f79e47fcec43533a429b7d59`; prior draft head `02f1050adbfb792953798d65d314d7560256c2f3`; verified starting main `9ae5f98e44739c6a7a645e41527e6fa016d6fc63`.

## 2026-07-17 — Codex — TOC capture clean-checkout binding hardened

- **Who:** Codex, at Corey Hartin's direction.
- **What:** PR #21's first exact-head CI attempts failed safely in the checked-in synthetic subprocess with the fixed `capture_driver/binding_mismatch` diagnostic. Repository-only diagnosis established that setup-python's CPython wrote an untracked repository-local bytecode cache while importing the reviewed contract, before the driver's clean-worktree proof; Apple's system Python used an external cache prefix and therefore did not reproduce the failure by default. Both capture entrypoints now disable bytecode writes before repository-local imports, every capture-owned Python child uses `-B`, and the end-to-end regression forces an in-tree cache configuration and proves that neither `__pycache__` nor a `.pyc` survives in the synthetic checkout. The fixed diagnostic test remains nonleaking. The complete local migration suite passed 268 tests, and GitHub Actions run `29644597753`, associated with exact substantive head `308f9c82f380607bed052f924a4086fdae77f427`, passed Linux verification, macOS no-replace publication, and the PostgreSQL 17.6/18.4 synthetic lane. All annotation and restore gates remain `BLOCKED`, migration readiness remains `RED`, and no retry is authorized by this work. No retained export or completed evidence package was accessed, and no Lovable, Supabase, n8n, database, target, secret, deployment, or runtime was accessed or changed.
- **SHAs:** latest substantive draft PR #21 head `308f9c82f380607bed052f924a4086fdae77f427`; fixed-diagnostic predecessor `35ba9058a8e364579fe6fdfa11d8a75b1acabe5f`; prior STATE-only head `a47c3c0cc0bd54edcd254359864b13497621f763`; initial substantive head `9b9e30278a0ac40a086722d1baee6f0bd9a0275d`; verified starting main `9ae5f98e44739c6a7a645e41527e6fa016d6fc63`.

## 2026-07-17 — Codex — Reviewed offline TOC capture envelope drafted

- **Who:** Codex, at Corey Hartin's direction.
- **What:** Opened draft PR #21 from freshly verified main `9ae5f98e44739c6a7a645e41527e6fa016d6fc63`. The new high-level driver validates every approved canonical/evidence/checkpoint/tool/count/path/capacity binding, invokes only the existing strict ZIP normalizer and low-level bounded TOC capture, and publishes one private no-replace opaque key/index/raw-TOC package. The derived mode-`0400` inner archive and normalization metadata are removed and directory-fsynced before success. Planted normalization, identity, capture, publication, cleanup, fsync, concurrent mutation, path replacement, permission, ownership, collision, capacity, timeout, output-cap, and rollback failures either clean completely or retain an explicit private quarantine/indeterminate stop; downstream annotation rejects root-level stop artifacts. Public diagnostics are fixed and nonleaking. The capture creates no annotation ledger and no restore command, and it stops at `REVIEW_REQUIRED` / `ANNOTATION_REQUIRED` with both restore gates `BLOCKED`. Local synthetic validation passed 268 migration tests, 70 security tests, 113 inspector checks, 26 target-safety checks, three cron target-safety checks, syntax, current-tree scan, and diff checks; remaining platform/application/database checks are exact-final-head CI gates. The retained export and completed evidence package were not opened, enumerated, statted, hashed, read, copied, normalized, or otherwise accessed, and no Lovable, Supabase, n8n, database, target, secret, deployment, or runtime was accessed or changed.
- **SHAs:** verified starting main `9ae5f98e44739c6a7a645e41527e6fa016d6fc63`; draft PR #21 substantive head `9b9e30278a0ac40a086722d1baee6f0bd9a0275d`.

## 2026-07-17 — Codex — PR #20 commit and CI metadata corrected

- **Who:** Codex, at Corey Hartin's direction.
- **What:** Corrected mutable CURRENT STATE and the PR description without changing tooling, tests, fixtures, workflows, or other substantive files. PR #20's existing four commits are `85821dd7c4b4613582d39e04daff80301277e64c` (initial substantive implementation), `653dbbc4213e6d86d342ee26f0a57276fa51dfdb` (intermediate STATE-only), `09c22a7cbd7c37f2b6f1a207cbe3d6243f90e2fa` (transport-hardening substantive), and `b52fa791297f41162c020c30712072b8851a2b22` (prior final STATE-only head); this documentation-only correction is the fifth PR commit, whose SHA is intentionally left to live Git metadata rather than self-reference. GitHub Actions run `29612072387` succeeded while associated with exact PR head `b52fa791297f41162c020c30712072b8851a2b22`, but Actions checked out synthetic merge commit `df8bd1425519ca9e0bb755d028d2fc35d0abb963`. The workflow's explicit `git diff --check` separately inspected the exact `30d5a46c30a851d89957fcfba0cc0a5a53df24d2...b52fa791297f41162c020c30712072b8851a2b22` base-to-head range. CI for subsequent heads is live GitHub metadata and must be checked directly rather than self-attested by this commit. This entry supersedes only the prior entries' then-current CI-pending interpretation; those immutable entries remain unchanged. No retained export/evidence package, target, runtime, secret, or external/pre-existing database was accessed.
- **SHAs:** complete pre-correction chain `85821dd7c4b4613582d39e04daff80301277e64c` → `653dbbc4213e6d86d342ee26f0a57276fa51dfdb` → `09c22a7cbd7c37f2b6f1a207cbe3d6243f90e2fa` → `b52fa791297f41162c020c30712072b8851a2b22`; starting main `30d5a46c30a851d89957fcfba0cc0a5a53df24d2`.

## 2026-07-17 — Codex — Synthetic TOC transfer made portable and bounded

- **Who:** Codex, at Corey Hartin's direction.
- **What:** Exact-head PR #20 CI run `29589191865` passed the 12 static cross-major contracts but failed at the fixed `pg18_toc_parser` stage before the public result could distinguish TOC transfer from parsing. A repository-only disposable reproduction established that the generated private TOC existed and the conservative parser accepted it, while Docker's archive-copy operation failed on both the runner and local Docker boundary. The fixture now streams only the already size-capped synthetic TOC through `docker exec` into its private mode-0400 workspace, checks the host byte length exactly equals the in-container length, and never routes bytes to logs. Fixed sub-stages distinguish stream, permissions, unknown/malformed/duplicate/invalid TOC, missing expected fixture classes/data, and internal failures without relaying a TOC line or object name. Static contracts reject reintroduction of `docker cp`, require the byte-count binding, and retain every digest/platform/network/cleanup/non-publish guarantee. A minimal digest-pinned disposable 17.6-source/18.4-client reproduction reached `synthetic_toc_diagnostic=success`; 12 focused cross-major tests, shell syntax, and diff checks pass. A new exact-final-head CI run remains required. No retained export/evidence package, target, Lovable, Supabase, n8n, Auth, Storage, secret, staging/production runtime, or external/pre-existing database was accessed; local containers used synthetic data only and were removed.
- **SHAs:** latest substantive draft PR #20 head `09c22a7cbd7c37f2b6f1a207cbe3d6243f90e2fa`; prior state-only head `653dbbc4213e6d86d342ee26f0a57276fa51dfdb`; original substantive head `85821dd7c4b4613582d39e04daff80301277e64c`; starting main `30d5a46c30a851d89957fcfba0cc0a5a53df24d2`.

## 2026-07-17 — Codex — Private TOC review and PostgreSQL 18 fixtures drafted

- **Who:** Codex, at Corey Hartin's direction.
- **What:** Opened draft PR #20 from verified main after the operator-reported retained-export aggregate reached `REVIEW_REQUIRED` with restore planning still `BLOCKED`. The new repository-only layer does not relax or replace the conservative aggregate parser: a future separately authorized capture retains exact raw `pg_restore --list` bytes privately, cross-binds them to the archive/evidence/checkouts/procedure/tool identities, and assigns keyed opaque entry IDs without exposing names. A deterministic private ledger accepts only the six reviewed classifications, requires exact entry/data-parent/dependency/managed-domain/disposition coverage, rejects malformed or ambiguous evidence, and can reach only `ELIGIBLE_FOR_HUMAN_REVIEW`; it never generates a restore command, while `restore_command_gate` stays `BLOCKED` and migration readiness stays `RED`. Private output uses owner-only directories, read-only files, no-replace atomic publication, fsync, cleanup, and indeterminate quarantine; detailed TOC/ledger material is Git-ignored. A separate digest-pinned official PostgreSQL image contract defines a disposable synthetic 18.4 client dumping a 17.6 source, offline bounded `pg_restore 18.4 --list`, conservative TOC parsing, and narrowly asserted restores to separate 17.6 and 18.4 targets. Documentation preserves PostgreSQL's no-guarantee boundary for loading newer-dump output into an older server and cites only official PostgreSQL/Supabase sources. Local repository-only checks passed 223 migration unit tests, 70 security tests plus a zero-output current-tree scan, 113 inspector tests, 26 target-safety tests, three cron target-safety tests, 11 Python compilations, shell syntax, and diff checks. Node/npm/Deno and GNU `timeout` are unavailable locally, so application/typecheck/Deno/audit and the live cross-major container lane remain exact-final-head CI gates. The retained export and durable package were not opened, listed, statted, hashed, copied, normalized, or otherwise accessed; no target, Lovable, Supabase, n8n, Auth, Storage, runtime, secret, or external/pre-existing database was accessed or changed.
- **SHAs:** verified starting main `30d5a46c30a851d89957fcfba0cc0a5a53df24d2`; draft PR #20 substantive head `85821dd7c4b4613582d39e04daff80301277e64c`.

## 2026-07-17 — Codex — Exclusion-index and deployment evidence corrected

- **Who:** Codex, at Corey Hartin's direction.
- **What:** Hardened open draft PR #19 in place after independent review. Both exact-index guards now require a non-exclusion index. The isolated PostgreSQL 17 integration plants a partial B-tree `EXCLUDE` constraint that otherwise matches the ordered keys and pending predicate, proves the installer rejects it, proves the concurrent preguard does not mistake it for the reviewed ordinary prerequisite, and then proves the exact ordinary index passes. Executor configuration, nondefault connection limit, password presence, and nonnull expiry are each planted independently before the privileged precondition and after it but before the postcondition; every case fails closed and rolls back the partial install and role drift. Repository language now states only that no tracked caller targets the strict slug, no deployment was authorized or observed, and production deployment state is unavailable. Only the legacy leaf handler/index blobs match base main; shared authentication/HEAD dependencies changed. Deploy-all is prohibited, future authorization may select only the strict slug after its gate clears, and the sender predicate is expressly a synthetic/procedural model rather than a runtime or control-plane lock. The prior immutable entry's stronger unused/deployment, bundle-identity, and general reachability implications are superseded by this correction. Commit `602f62a...` changed 32 raw pathnames represented by 31 diff records; the complete PR still has a 29-file base-to-head scope. Failed PR-event run `29549990324` was triggered by intermediate STATE-only branch head `474b51c0...`; successful corrected run `29550941314` was triggered by `e9af9461...`. GitHub Actions exercised each synthetic PR merge tree against base `0275d879...`; only the explicit diff check inspected the exact base-to-head range. Every runtime, migration, index, role, receiver deployment, sender activation, controlled dispatch, handoff, pruning, and production-rotation gate remains BLOCKED. Local tests used repository fixtures and one dedicated disposable PostgreSQL 17 container only; no external/pre-existing database or runtime system was accessed or changed.
- **SHAs:** latest substantive head `16b05b6c2ef34d84edf14ae73f0289d7bf8a006a`; prior corrected STATE head `e9af9461cadf274c9e9e191c1972856214b53103`; automatic-grant correction `058462d93e3ec7bd628620e0beb2b007c951c66a`; intermediate STATE-only head `474b51c0e9b025e8067cefd5e5793ec87036a7e9`; primary hardening head `602f62a9748ae150fe5c29171fa17e2663e92486`; starting main `0275d879148c3299d54846333d304c2ac8476b64`.

## 2026-07-16 — Codex — PG17 automatic-grant fixture corrected

- **Who:** Codex, at Corey Hartin's direction.
- **What:** Exact-head PR #19 CI run `29549990324` passed macOS plus repository security, typecheck, and application tests on Linux, then failed in the new receipt integration because its catalog assertion incorrectly required `$PGUSER` to be the grantor of PostgreSQL 17's automatic non-superuser `CREATEROLE` membership. PostgreSQL documents and the isolated fixture confirms that the bootstrap superuser is the grantor. The focused correction removes the login-name assumption while retaining and directly checking one automatic edge with `ADMIN TRUE`, `SET FALSE`, `INHERIT FALSE`, a bootstrap-superuser grantor, zero-edge incompatibility, unchanged function ownership, and failed owner transfer. The full receipt integration then passed through a distinct synthetic `runner` superuser login in the labeled local PG17 fixture. No runtime gate changed and all deployment/index/install/sender/dispatch/handoff/pruning/rotation gates remain BLOCKED.
- **SHAs:** correction substantive head `058462d93e3ec7bd628620e0beb2b007c951c66a`; primary hardening head `602f62a9748ae150fe5c29171fa17e2663e92486`; failed prior STATE head `474b51c0e9b025e8067cefd5e5793ec87036a7e9`; starting PR head `2a456b1ace5e0df4948dd540145f53f0dc985156`.

## 2026-07-16 — Codex — Cron receipt install and strict receiver isolated

- **Who:** Codex, at Corey Hartin's direction.
- **What:** Hardened open draft PR #19 in place without runtime access. The strict attempt-receipt receiver now lives at the unused `run-strategy-task-reaper-receipt-v1` slug while the existing `run-strategy-task-reaper` handler/index remain byte-identical to main and no-attempt compatible. The receipt SQL moved outside `supabase/migrations` into a separately authorized all-file installer template, so Lovable cannot automatically consume a guaranteed index/role failure. Its exact index guard requires a valid/ready/live nonunique btree with exactly `(updated_at, id)`, no included columns, and the exact pending predicate. A real non-superuser `CREATEROLE` PG17 fixture reproduces the automatic `ADMIN TRUE / SET FALSE / INHERIT FALSE` creator edge and failed owner transfer, while the accepted pre-provisioned path binds role flags, memberships/grantors/options, settings, default and initial ACLs, ownership, direct ACL grantors/grant options, and required/forbidden effective privileges. Planted grant-option, alternate-grantor, ownership, membership, default-ACL, role-flag, privilege, policy, and late-install faults fail closed. A sequence that does not roll back proves a first row trigger ran before the second failed; all task fields, transactional audit rows, and success/no-op receipts roll back. The exact sender predicate rejects active-capable, inactive-incapable, missing, extra, duplicate, and substituted tuples; no pre-activation state can target the strict receiver actively. The environment contract is correctly described as request-local and per-key memoized. All runtime/index/install/deployment/sender/dispatch/handoff/pruning/rotation gates remain BLOCKED. Local checks passed 69 security tests, a zero-output current-tree scan, 162 migration tests, 1,919 application tests, typecheck, target-safety checks, and the isolated PostgreSQL 17 integration; Deno, audit, and macOS remain exact-final-head CI gates. No index or role was created outside the isolated fixture, and no external runtime, database, credential, deployment, function invocation, rotation, retained export, Lovable, Supabase, or n8n system was accessed or changed.
- **SHAs:** latest substantive draft PR #19 head `602f62a9748ae150fe5c29171fa17e2663e92486`; starting PR head `2a456b1ace5e0df4948dd540145f53f0dc985156`; starting main `0275d879148c3299d54846333d304c2ac8476b64`.

## 2026-07-16 — Codex — PR #19 Deno HEAD-boundary test typing corrected

- **Who:** Codex, at Corey Hartin's direction.
- **What:** Exact-head CI run `29543370141` at prior STATE head `baa981ad5e3d14f5ccd04e0b765c413c3d875545` passed repository security, typecheck, all 1,917 application tests, migration/PostgreSQL integrations, Deno check, and the macOS inspection workflow, then failed closed during Deno test type-checking because the shared `HEAD` proof supplied the generic two-argument business-handler factory directly to the reaper's new four-argument attempt-aware factory. The correction adds a fully typed test-only adapter that forwards request, cron classification, and the request-local environment while deliberately ignoring the attempt context in this non-reachability proof. If `HEAD` ever reaches business work, the same ledger-backed callback still records database/model/fetch/business calls and the test fails. Focused rollout-contract tests remain 24/24 green; local Deno is unavailable, so the corrected type check and runtime test remain exact-final-head CI gates. Runtime receiver code and every RED/BLOCKED gate are unchanged. No external runtime, database, credential, deployment, invocation, rotation, or retained export was accessed.
- **SHAs:** latest substantive head `89e713546dcccb660db55c1703f461b71345e651`; prior STATE head `baa981ad5e3d14f5ccd04e0b765c413c3d875545`; starting PR head `2a987558a799d00a80846f96a987462c1948754e`.

## 2026-07-16 — Codex — Cron receipt rollout and executor boundary hardened

- **Who:** Codex, at Corey Hartin's direction.
- **What:** Hardened open draft PR #19 in place using repository code, synthetic fixtures, and isolated local PostgreSQL 17 databases only. Attempt-domain separation now directly checks both configured receiver slots plus the presented cron, gateway API/JWT/authorization, and service-role domains before semantic hashing or client/RPC creation; the same request-local environment snapshot supplies authentication, project binding, and the exact URL/key used by client construction, with concurrent snapshot-isolation and zero-client/RPC planted tests. The future production handoff now keeps `RECEIVER_DEPLOYMENT_GATE` blocked while any active caller lacks attempt-ID support and orders pause/drain, a separately indexed all-file receipt migration, inactive attempt-capable replacements, strict receiver deployment, one controlled correlated dispatch, promotion/rejection proof, and all-or-none final activation. The receipt migration no longer runs ordinary `CREATE INDEX`; a separately authorized PostgreSQL-17 concurrent template or explicit maintenance window is required. Its dedicated non-superuser owner now has an exact zero-membership/capability/column/private-state/RLS contract. PostgreSQL plants remove every required privilege, add broader INSERT/REFERENCES/private authority, mutate both membership directions and owner flags, alter fixed policies including permissive PUBLIC access, and inject a late migration failure; every case leaves no business effect or false success/no-op. A real two-distinct-attempt race produces one transition, one applied receipt, one legitimate no-op, and no duplicate effect. Protocol-v1 receipts remain retained while v1 is accepted, with no cleanup path installed. Local checks passed 66 security tests, 24 rotation-contract tests, 1,917 application tests, typecheck, tracked-tree scanning, repository inventory checks, target-safety guards, and PostgreSQL 17 receipt/catalog/service/raw-dump integrations. Deno and the complete high-level inspection integration remain exact-final-head CI gates. `daily-digest` and `schedule-daily-plan` remain RED; application proof remains `PARTIAL_BLOCKED`, and every controlled-SQL, receiver/index/pruning, sender, dispatch, handoff, and production-rotation gate remains BLOCKED. No real credential, function, deployment, invocation, rotation, Lovable/Supabase/n8n runtime, external or pre-existing database, staging, production, secret store, or retained export was accessed or changed.
- **SHAs:** draft PR #19 latest substantive head `7d5f75d1a39de0789423c43c67d3a187b5c19ea8`; prior PR head `2a987558a799d00a80846f96a987462c1948754e`; starting main `0275d879148c3299d54846333d304c2ac8476b64`.

## 2026-07-16 — Codex — Receipt function deployment inventory refreshed

- **Who:** Codex, at Corey Hartin's direction.
- **What:** The strict parser correction changed the reviewed dependency closure for `run-strategy-task-reaper`; the repository artifact test correctly rejected the stale recorded fingerprint before Deno. `docs/migration/edge-functions.json` was regenerated from repository source with a truthful `2026-07-16T17:42:24Z` collection time, changing only the aggregate repository-content identity and that receiver's deployment-closure fingerprint. Deterministic regeneration now passes. This is repository evidence, not observed runtime configuration or deployment proof. No gate, external runtime, credential, retained export, database, deployment, or rotation was accessed or changed.
- **SHAs:** draft PR #19 latest substantive head `3c81efe1ab340b14fece1595e14a6899ad2c1671`; receipt consistency correction `824543ee3d089935332bd7e38201b9e1459e0ae4`; receipt implementation `82048a9f8d70af2951de91cb3d8e6aab53436430`; starting main `0275d879148c3299d54846333d304c2ac8476b64`.

## 2026-07-16 — Codex — Present receipt consistency fails closed at parse boundary

- **Who:** Codex, at Corey Hartin's direction.
- **What:** Exact-head Deno executed 86 tests and showed that the strict receipt parser accepted an otherwise well-shaped present receipt with a false identity or effect consistency flag, although the later execution layer still rejected it. The parser now requires both consistency booleans to be true whenever `attempt_present` is true; the separately reviewed absent/indeterminate tuple remains the only false-consistency shape. The existing planted cases directly cover each false flag and arbitrary-field/sentinel suppression. This narrows accepted evidence without changing SQL effects, sender behavior, transaction boundaries, or any BLOCKED gate. No external system, credential, retained export, deployment, database, or rotation was accessed or changed.
- **SHAs:** draft PR #19 latest substantive head `824543ee3d089935332bd7e38201b9e1459e0ae4`; prior Deno test-type correction `4a377801392d35e8d182a6432f4fdb48e31f0567`; receipt implementation `82048a9f8d70af2951de91cb3d8e6aab53436430`; starting main `0275d879148c3299d54846333d304c2ac8476b64`.

## 2026-07-16 — Codex — Cron receipt credential-header tests made type-exact

- **Who:** Codex, at Corey Hartin's direction.
- **What:** Exact-head Linux CI passed repository security, application/typecheck, 162 migration tests, all inspector checks, catalog/service integrations, the real PostgreSQL 17 dump workflow, and the new PostgreSQL 17 receipt integration, then Deno's test type-checker rejected an inferred union whose absent object-literal keys became optional `undefined` header values. The four planted credential-equality header cases now carry an explicit read-only string-record type. This changes no runtime code or assertion and allows Deno to verify the intended `HeadersInit` contract without coercion. The macOS job was already green. No gate or runtime status changed, and no external system, credential, retained export, deployment, database, or rotation was accessed or changed.
- **SHAs:** draft PR #19 latest substantive head `4a377801392d35e8d182a6432f4fdb48e31f0567`; prior real-archive fixture correction `d49acda793b3cd634344d1d12a2ec0f0b91c9039`; receipt implementation `82048a9f8d70af2951de91cb3d8e6aab53436430`; starting main `0275d879148c3299d54846333d304c2ac8476b64`.

## 2026-07-16 — Codex — Real-archive CI fixture preserves inspection baseline

- **Who:** Codex, at Corey Hartin's direction.
- **What:** The corrected draft PR #19 macOS job and all 162 Linux migration tests passed, and the new PostgreSQL 17 cron-receipt integration passed, before the Linux real-archive high-level fixture exposed the same inherited-application-migration mismatch. That CI-only integration now builds a clean synthetic approved descendant checkout containing current reviewed tools/configuration plus the exact historical migration tree, then asserts the tree remains identical to `c87a124602eb669b3ec5a3829610c6cb465d3e26` before inspecting its synthetic `pg_dump -Fc` fixtures. Shell syntax and diff checks pass locally; the PostgreSQL execution remains an exact-final-head CI gate because the local safety guard correctly refused a TCP test connection rather than weakening its Unix-socket/container boundary. The production inspection workflow and baseline guard are unchanged. Receipt implementation and every BLOCKED production gate are unchanged; no real credential, external/pre-existing database, retained export, deployment, runtime, or rotation was accessed or changed.
- **SHAs:** draft PR #19 latest substantive head `d49acda793b3cd634344d1d12a2ec0f0b91c9039`; prior fixture correction `5bf729a8ec0f820b67a34dc7e089f142d7d56a38`; receipt implementation `82048a9f8d70af2951de91cb3d8e6aab53436430`; starting main `0275d879148c3299d54846333d304c2ac8476b64`.

## 2026-07-16 — Codex — Cron receipt PR preserves inspection migration baseline

- **Who:** Codex, at Corey Hartin's direction.
- **What:** Exact-head CI for draft PR #19 exposed that the migration-evidence workflow's synthetic checkout inherited the newly added application migration and therefore correctly failed its historical migration-baseline guard before every scenario. The test fixture now reconstructs the exact `c87a124602eb669b3ec5a3829610c6cb465d3e26` migration tree while retaining current reviewed inspection tools, asserts the resulting synthetic checkout is byte-identical to that baseline for `supabase/migrations`, and preserves planted tracked, ordinary-untracked, and ignored-untracked migration-drift failures. All 59 evidence-workflow tests pass locally. The production workflow and its fail-closed historical baseline were not relaxed. This correction changes no receiver, migration, runtime, gate, or deployment status; application proof remains `PARTIAL_BLOCKED` and every controlled-SQL, sender, dispatch, handoff, and production-rotation gate remains BLOCKED. No external runtime, credential, pre-existing database, retained export, deployment, or rotation was accessed or changed.
- **SHAs:** draft PR #19 latest substantive head `5bf729a8ec0f820b67a34dc7e089f142d7d56a38`; receipt implementation substantive head `82048a9f8d70af2951de91cb3d8e6aab53436430`; starting main `0275d879148c3299d54846333d304c2ac8476b64`.

## 2026-07-16 — Codex — Durable cron-attempt receipt boundary added for task reaper

- **Who:** Codex, at Corey Hartin's direction.
- **What:** Opened draft PR #19 from verified main `0275d879148c3299d54846333d304c2ac8476b64` using repository code, synthetic fixtures, and an isolated local PostgreSQL 17 database only. The cron-authenticated `run-strategy-task-reaper` path now requires one strict canonical attempt header after cron authentication, binds it to fixed receiver/protocol/environment/project/fingerprint identity, and executes its bounded deterministic `task_runs` transition plus terminal receipt in one fixed service-role-only PostgreSQL transaction. Exact retry and concurrent duplicate tests produce at most one effect and the same durable receipt; identity conflicts fail closed; a transaction failure rolls back every effect before a fixed known-failure receipt; a terminal-write failure leaves neither receipt nor effect; a lost response is safely replayed; legitimate no-op and stale nonterminal states remain distinct. Authenticated `HEAD` still performs no business/receipt/database/model/fetch work, and the separate `daily-digest` end-user JWT path is unchanged. A protected-environment verifier emits only reviewed receipt fields. Real PG17 tests cover transaction, concurrency, rollback, ACL/RLS, semantic parity, and planted nonleakage; repository security tests and the current-tree scanner are green, while application, typecheck, Deno, audit, migration, inspector, target-safety, PostgreSQL, and macOS checks remain exact-final-head CI gates. `daily-digest` and `schedule-daily-plan` remain RED at their documented non-atomic boundaries, so application proof is only `PARTIAL_BLOCKED` and every controlled-SQL, sender, dispatch, legacy-handoff, and production-rotation gate remains BLOCKED. No real credential, external/pre-existing database, function, deployment, rotation, Lovable/Supabase/n8n runtime, staging, production, secret store, or retained export was accessed or changed.
- **SHAs:** starting main `0275d879148c3299d54846333d304c2ac8476b64`; draft PR #19 substantive head `82048a9f8d70af2951de91cb3d8e6aab53436430`.

## 2026-07-16 — Codex — Cron verifier type boundary and final-enable contract hardened

- **Who:** Codex, at Corey Hartin's direction.
- **What:** Applied one final repository-only hardening pass to open draft PR #18. The exported verification function now snapshots every runtime field as unknown, requires primitive strings for environment, phase, accepted slot, URL, accepted secret, rejected control, API key, and optional JWT, and revalidates all routing/attestation enums before URL parsing, header construction, or fetch. Planted direct-call cases include numeric `123` versus string `"123"`, two separately boxed equal strings, arrays, coercible objects, boxed gateway inputs, and invalid environment/phase/slot values; every case records zero fetches. The future production state model now inserts `RECEIVER_PROMOTED_PREDECESSOR_REJECTED` only after promoted-key acceptance, `CRON_SECRET_NEXT` removal, predecessor rejection, and continued job inactivity. Final activation is defined as one serializable, cron-mutation-fenced transaction that revalidates legacy-ID absence, the exact three replacement identities and uniqueness, wrapper commands/fingerprints, plaintext absence, receiver-promotion evidence, and all-inactive state; it activates all three, repeats the full predicate plus all-active state, and commits only then. Synthetic partial activation, second/third-job failure, pre- and post-activation identity/duplicate/fingerprint/plaintext/legacy-ID/receiver/concurrency drift all return the proven all-inactive replacement set. An independent read-only audit caught and prompted correction of an initial synthetic false-green that did not repeat the full predicate after activation. All six fixed application-receipt, controlled-SQL, sender, dispatch, handoff-mutation, and production gates remain byte-for-byte unchanged and BLOCKED; no receipt was implemented and the future model authorizes no action. Local repository checks passed 60 security tests, the zero-output current-tree scan, 162 migration Python tests, raw-inspector and target-safety checks, shell syntax, and diff checks. Deno, application/typecheck, PostgreSQL integration, audit, and macOS remain exact-final-head CI gates. The official public Supabase changelog was read as required by the repository workflow skill; no Supabase/Lovable/n8n project or runtime, real credential, function, external/pre-existing database, retained export, staging, or production system was accessed or changed, and no pause, invocation, deployment, rotation, or mutation occurred.
- **SHAs:** latest substantive draft PR #18 head `6801637f8ffe7e4b38ece263aa63237f161bf17b`; prior final draft head `618fad71739daa8453bd875278195760a77437a5`; starting main `b6cb6816b3f2501a8436b36f1b20aedc2798018f`.

## 2026-07-16 — Codex — Cron rotation production handoff blocked safely

- **Who:** Codex, at Corey Hartin's direction.
- **What:** Hardened open draft PR #18 in place using repository code, documentation, and synthetic fixtures only. The environment-only harness now requires the accepted cron secret, rejected control, gateway API key, and optional gateway JWT to have distinct canonical visible-ASCII wire representations before any fetch; all six equality pairs, HTTP-whitespace/control normalization aliases, environment loading, and nonleaking canonical failures are planted negative cases. The recurring Vault sender template checks every domain it supplies before `net.http_post`; a separate non-executable controlled-probe template resolves all four domains, requires a JWT whenever reviewed expected `verify_jwt` is true, and remains owner-only and blocked pending one-shot role/grant enforcement. CI now binds the harness's reviewed expected three-function JWT map to both `supabase/config.toml` and `docs/migration/edge-functions.json` with duplicate-key/component rejection; this does not observe runtime configuration. Corey-supplied sanitized production evidence identifies active legacy jobs `7`, `9`, and `15` with plaintext cron-header construction and no Vault use. The documented future state machine pauses/drains, preserves each job's name/schedule/owner/database/function-slug mapping through a distinct inactive fixed wrapper, atomically retires old IDs without committed duplicate schedules, rejects residual plaintext, and never activates a schedule for its future controlled direct-wrapper probe. A deterministic synthetic model exercises the blocked entry gate, identity/endpoint drift, races, rollback, duplicates, retained IDs, plaintext, and dispatch-count invariants, but it does not prove installed pg_cron, lock, privilege, or production semantics. No exact durable application receipt exists for success, failure, duplicate attempt, or legitimate no-op behavior, so controlled SQL remains template-only and sender activation, controlled dispatch, legacy-handoff mutation, and production rotation all stop before `LEGACY_PAUSED`. The Phase 3.6 application test has been relabeled honestly as a lexical source-binding guard; it does not prove call reachability, dependency closure, or deployment. Local checks passed 57 security tests, the zero-output current-tree scan, 162 migration Python tests, 113 raw-inspector checks, 26 target-safety checks, eight Python compilations, shell syntax, and diff checks. Node and Deno are unavailable locally; application, typecheck, Deno, audit, PostgreSQL integration, and macOS checks remain exact-final-head CI gates. No real credential was read, compared, hashed, rotated, or recorded; no function was invoked; and no deployment, runtime mutation, external/pre-existing database access, Lovable/Supabase/n8n operation, retained-export access, or production/staging action occurred.
- **SHAs:** latest substantive draft PR #18 head `c0247193c12db88f23982aadd50d9a3898fc97a7`; prior draft head `da26858195599757b74084a350d5485a1410cf64`; starting main `b6cb6816b3f2501a8436b36f1b20aedc2798018f`.

## 2026-07-16 — Codex — Cron auth security guard made closure-aware

- **Who:** Codex, at Corey Hartin's direction.
- **What:** Exact-head CI run `29492888345` passed the macOS job, tracked-schema credential scan, setup, and application typecheck, then stopped after 1,914 application tests passed and two Phase 3.6 assertions treated `run-strategy-task-reaper` as unauthenticated. The runtime code was not missing authentication: the hardened refactor had moved the reviewed boundary from `index.ts` into a local handler and shared receiver, while the application security contract inspected only raw `index.ts` patterns. The contract now recognizes only three exact reviewed factory/handler/server-call bindings and requires each local handler to delegate to the shared receiver, the shared receiver to invoke `hasValidCronSecret` before `HEAD` or business dispatch, and the auth module to read both approved receiver slots. This closes the test's deployment-closure blind spot without adding a marker, weakening auth, broadening a public allowlist, or changing runtime behavior. The failed run skipped later Linux migration, Deno, and audit gates, so a new exact-final-head Linux/macOS run remains required. No real credential, function, deployment, external/pre-existing database, Lovable/Supabase/n8n runtime, or retained export was accessed or changed.
- **SHAs:** closure-aware latest substantive draft PR #18 head `af8fd9b1451f4b74eddec69ce81270e29f9d90a2`; gateway/evidence substantive head `137346afb6628ef65f1f7fac81680fca91d900fb`; failed state head `d805917c3dde6dfacc927838a9c30351bfb43f8a`; starting main `b6cb6816b3f2501a8436b36f1b20aedc2798018f`.

## 2026-07-16 — Codex — Cron rotation gateway and evidence contracts hardened

- **Who:** Codex, at Corey Hartin's direction.
- **What:** Hardened existing draft PR #18 in place. The sender template and environment-only harness now treat a gateway API key and a gateway JWT as independent, unequal inputs: the API key is sent only as `apikey`, reviewed `verify_jwt=true` functions require a separately supplied JWT, and reviewed `verify_jwt=false` functions omit Authorization. The procedure now selects exactly one caller branch and forbids creating a scheduler for staging solely because its sanitized inventory reported zero jobs. Three repeated accepted/rejected `HEAD` pairs require `204`/`401` and `Cache-Control: no-store`; phase and accepted-slot labels are explicitly operator attestations. A shared handler boundary plus synthetic ledgers prove valid, invalid, and missing `HEAD` requests return before injected business, database, model, or fetch work. Sanitized SQL checks cover fixed wrapper ownership, ACL, `SECURITY DEFINER`, `proconfig`, definition fingerprint, cron-run evidence, request/attempt/job correlation, pg_net transport, and application effects while retaining a blocking ceiling when cron-run-to-attempt linkage is only inferred. The deterministic Edge Function dependency inventory was regenerated with its actual UTC collection time. Repository-local checks passed 40 security tests, a zero-output current-tree scan, 162 migration tests, 113 raw-inspector checks, 26 target-safety checks, shell/Python syntax, and diff checks; Deno, application, typecheck, audit, PostgreSQL integration, and macOS coverage remain exact-final-head CI gates. No real credential was rotated or accessed, no function was invoked, and no deployment, external/pre-existing database access, Lovable/Supabase/n8n operation, retained-export access, or runtime change occurred; CI database coverage remains confined to synthetic fixtures. Both environment-specific external rotations remain open.
- **SHAs:** latest substantive draft PR #18 head `137346afb6628ef65f1f7fac81680fca91d900fb`; prior PR #18 head `76e64484cf64f067f770e5097d2372b36e3f8d76`; starting main `b6cb6816b3f2501a8436b36f1b20aedc2798018f`.

## 2026-07-16 — Codex — Independent cron-secret rotation readiness drafted

- **Who:** Codex, at Corey Hartin's direction.
- **What:** Opened draft PR #18 after verifying and branching from main `b6cb6816b3f2501a8436b36f1b20aedc2798018f`. A category-only repository audit found three direct `CRON_SECRET` consumers and no tracked runtime sender or secret producer. The receivers now independently evaluate `CRON_SECRET` and `CRON_SECRET_NEXT`, fail closed when neither matches, and expose an authenticated `HEAD` probe that returns before application, model, or database work. A checked-in harness accepts values only from the runtime environment, pins each reviewed environment to its exact project host and an allowlisted function slug, sends only `HEAD`, never reads a response body, and emits only fixed non-secret status evidence. The separate staging and production procedures use different named actors and require receiver-first overlap, stable-name Vault indirection proven on the target, exact cron-job locking/no-replace gates, sanitized pg_net response metadata plus deterministic application effects, predecessor rejection, rollback, and retirement. Metadata-only connector calls read the user-owned dynamic-staging project/function/extension inventory and a command-free cron inventory, which returned zero jobs; no secret-management write surface was available or tested. A harmless production project-metadata retry returned permission denied and ended production discovery. Local repository-only validation passed 36 security tests, a zero-output current-tree secret scan, 162 migration tests, 113 raw-inspector checks, 26 PostgreSQL target-safety checks, shell/Python syntax, and diff checks. Pinned Deno, application, typecheck, audit, synthetic PostgreSQL, and macOS checks remain final-head CI gates. No secret value was retrieved, compared, printed, hashed, fingerprinted, or recorded; no Edge Function was invoked; and no credential, Vault entry, cron job, function, database, deployment, Lovable/n8n state, retained export, business data, staging, or production runtime was changed. Both incident credentials remain unrotated and externally blocked pending separate authorization.
- **SHAs:** starting main `b6cb6816b3f2501a8436b36f1b20aedc2798018f`; draft PR #18 substantive head `409b43acfaa90b9f75ff49147299ad98a844c8de`.

## 2026-07-16 — Codex — Invalid INSERT grammar claim corrected

- **Who:** Codex, at Corey Hartin's direction.
- **What:** Corrected the immediately preceding immutable PR #17 entry, which inaccurately described `INSERT INTO ONLY` as valid PostgreSQL syntax. That entry remains below for append-only provenance but is superseded by this correction. PostgreSQL INSERT takes the target relation directly after `INTO`; the scanner again permits optional `ONLY` only for valid UPDATE and DELETE forms, and the four invalid INSERT regressions were removed. No valid PostgreSQL security gap was established or closed by the superseded change. Original-text executable-cron matching remains because a separate valid, fully quoted and comment-separated `UPDATE ONLY` regression is detected in the original text but erased by the reviewed normalization pass; its planted payload and filename sentinels do not escape. The known-pattern/non-exhaustive-parser ceiling is unchanged. Local repository-only validation passed 36 security tests, the no-output current-tree scan, 162 migration Python tests, 113 raw-inspector checks, 26 target-safety checks, shell/Python syntax, and diff checks. Final-head application, typecheck, Deno, audit, isolated synthetic PostgreSQL 17 integration, and macOS checks remain CI gates. No historical PR patch or blob, Lovable, Supabase, n8n, staging, production, secret store, external/pre-existing database, retained export, restore, deployment, rotation, migration, or data movement was accessed or performed.
- **SHAs:** corrected substantive draft PR #17 head `57c34cd283c05df8085724a7cea379bd059938a7`; superseded substantive head `20a218a79a49d651ad6f89091727d866a89ac7bf`; prior final head `34ff20e159926a02b7d599d84257cad880e8fb6a`; starting main `1b13483be43b3d5f28a7086606b8a921a6879f18`.

## 2026-07-15 — Codex — INSERT INTO ONLY cron guard closed

- **Who:** Codex, at Corey Hartin's direction.
- **What:** Hardened draft PR #17's existing known-pattern scanner so direct cron-table mutation detection accepts PostgreSQL's optional `ONLY` keyword after `INSERT INTO`, using the same bounded comment/whitespace token handling already used for `UPDATE` and `DELETE`. Executable-cron matching now evaluates the same reviewed patterns against both original and reviewed-normalized text so the normalization pass cannot erase a fully comment-separated quoted relation; no identifier rule, finding class, file scope, or exhaustive-parser claim was added. Four planted forms—plain, mixed-case, quoted relation, and comments/whitespace between every relevant token—each return only `executable_cron_in_derived_snapshot`, while distinct payload and filename sentinels remain absent from captured stdout and stderr. Local repository-only validation passed 36 security tests, the no-output current-tree scan, 162 migration Python tests, 113 raw-inspector checks, 26 target-safety checks, shell/Python syntax, and diff checks. Final-head application, typecheck, Deno, audit, synthetic PostgreSQL 17 integration, and macOS checks remain CI gates. No historical PR patch or blob, Lovable, Supabase, n8n, staging, production, secret store, external/pre-existing database, retained export, restore, deployment, rotation, migration, or data movement was accessed or performed.
- **SHAs:** latest substantive draft PR #17 head `20a218a79a49d651ad6f89091727d866a89ac7bf`; prior final head `a12ada9f50937c85f2416d3219f38a2dfd4812eb`; starting main `1b13483be43b3d5f28a7086606b8a921a6879f18`.

## 2026-07-15 — Codex — Credential-containment evidence boundary corrected

- **Who:** Codex, at Corey Hartin's direction.
- **What:** Hardened draft PR #17 after independent verification established that the public GitHub patch API disclosed predecessor bytes despite the prior diff-rendering hint. The ineffective hint and its test are removed; the PR patch, Git history, forks, caches, archived patches, and prior clones are now explicitly treated as persistent exposure, with external staging-secret rotation still required and not performed. This entry corrects the earlier session statement: only the derived repository snapshot became non-executable; no runtime schedule was queried, disabled, removed, recreated, or changed. Historical repository evidence records nine snapshot schedules total, one migration-defined and eight additional. Prior independent repository verification—not the category-only scanner—classified the removed bearer material as anon/publishable rather than service-role authority. The scanner is now explicitly a known-pattern accidental-regression guard, not an exhaustive security boundary: it covers every tracked SQL file plus only five documented Supabase configuration suffixes, recognizes the reviewed qualified/search-path cron calls and direct `cron.job` mutations, catches planted split/escaped cron-header and bearer forms, and exposes no untrusted filename. The sanitized current snapshot passes the no-output category scan. Local repository-only validation passed 35 security tests, 162 migration Python tests, 113 raw-inspector checks, 26 target-safety checks, shell/Python syntax, and diff checks. Node/npm/Deno are unavailable locally, so application, typecheck, Deno, audit, and PostgreSQL integration checks remain final-head CI gates. Local work accessed no external or pre-existing database; CI's database checks use isolated synthetic PostgreSQL 17 fixtures. No Lovable, Supabase, n8n, staging, production, secret store, retained export, restore, deployment, rotation, migration, or data movement was accessed or performed.
- **SHAs:** corrected substantive draft PR #17 head `00680fdf0d766912bfc4be86ffa8791925e8f7d3`; prior substantive head `8068b7248a73f962e1bf5ee001fb2e300f5ddec6`; starting main `1b13483be43b3d5f28a7086606b8a921a6879f18`.

## 2026-07-15 — Codex — Dynamic-staging credential literals contained in draft

- **Who:** Codex, at Corey Hartin's direction.
- **What:** Opened draft PR #17 from independently verified main `1b13483be43b3d5f28a7086606b8a921a6879f18` for repository-only credential containment. A category-only preflight confirmed exactly three non-placeholder cron-header findings and repeated embedded bearer-JWT findings in the derived dynamic-staging schema without printing, quoting, decoding, hashing, fingerprinting, or retaining either value. The current tree removes those literals and the complete executable cron schedule block; schedules now remain explicitly disabled until a separately authorized deployment supplies reviewed configuration and an approved runtime secret source. A fail-closed tracked SQL/server-configuration scanner emits only path and allowlisted finding category, sanitizes Git inputs, uses descriptor-relative no-follow reads with identity revalidation, and has planted nonleakage regressions for alternate literal/config forms, bearer tokens, placeholder/default suffixes, runtime-setting assignments, cron-token comments/variants, partial roots, alternate indexes, symlinks, oversized inputs, and concurrent mutation/replacement. Local validation passed 26 security tests, the no-output current-tree scan, 162 migration Python tests, 113 raw-inspector checks, 26 target-safety checks, shell/Python syntax, and diff checks; application/typecheck/Deno/PostgreSQL-integration/audit validation remains required in CI because Node/npm/Deno are unavailable locally and no local database access was authorized. The removed bearer material was anon/publishable client material but remains inappropriate in cron SQL. External staging-secret rotation is still required, current-tree removal does not purge history/forks/caches/clones, and history rewriting is out of scope. No Lovable, Supabase, n8n, staging, production, secret store, database, retained export, restore, deployment, migration, or data movement was accessed or performed.
- **SHAs:** substantive draft PR #17 head `8068b7248a73f962e1bf5ee001fb2e300f5ddec6`; starting main and merged PR #16 main `1b13483be43b3d5f28a7086606b8a921a6879f18`.

## 2026-07-15 — Codex — PostgreSQL integration assertion aligned to provenance v6

- **Who:** Codex, at Corey Hartin's direction.
- **What:** Corrected one stale synthetic PostgreSQL 17 high-level integration assertion after final-head run `29429575704` passed macOS and all Linux gates through the catalog, service, and raw-dump integrations, then required provenance format version 5 even though the reviewed runtime-binding contract intentionally emits version 6. The test now requires version 6 exactly; an exhaustive executable migration-test search found no other hardcoded provenance-v5 expectation, and shell syntax plus diff checks pass. Corrected Linux and macOS final-head CI remain required. Migration readiness remains RED. The retained export was not accessed, statted, hashed, read, copied, renamed, chmodded, unzipped, normalized, or inspected; no Lovable, Supabase, production, secrets, external/pre-existing database, restore, deployment, migration, export, or data movement was accessed or performed.
- **SHAs:** runtime-binding substantive head `c30112ee594dac9af94d8e828681c9c89b082f03`; corrected latest substantive head `db8929215bd0134038372f9072e26a829725df84`.

## 2026-07-15 — Codex — Evidence publication bound to runtime truth

- **Who:** Codex, at Corey Hartin's direction.
- **What:** Hardened draft PR #16 in place with repository code and synthetic fixtures only. A frozen `PublicationExpectations` now binds the exact admitted canonical descriptor and the runtime checkout, tool, config, Python, source, timeline, normalizer, inner/header, report, and analysis identities before retained evidence construction. Publication compares provenance to that independent truth at pending, descriptor-staging, immediate pre-rename, post-rename/pre-completion, and completed-marker validation. Coherent fully rehashed substitutions that change redundant outer identities and checksum sidecars, checkout/tool/config/project claims, envelope metadata, or report/analysis claims remain internally self-consistent but fail publication; an exact runtime-matched positive package traverses every gate. The repository-migration name scan now reports only `CONSERVATIVE` when object references resolve and `INCOMPLETE` for unresolved aggregate evidence, never `COMPLETE`; it remains a non-exhaustive heuristic across aliases, pre-name modifiers, object-kind ambiguity, and dynamic SQL, with restore planning always `BLOCKED`. Provenance format is version 6. Local checks passed: 162 migration Python tests, 113 raw-inspector checks, 26 target-safety checks, eight shell syntax checks, seven Python no-write compilations, the six decisive runtime/SQL-form regressions, and diff checks. Node/npm/Deno are unavailable locally, so final Linux/PostgreSQL 17 and macOS CI remain required. Migration readiness remains RED. The retained export was not accessed, statted, hashed, read, copied, renamed, chmodded, unzipped, normalized, or inspected; no Lovable, Supabase, production, secrets, external/pre-existing database, restore, deployment, migration, export, or data movement was accessed or performed.
- **SHAs:** PR #16 prior final head `c6e5ff652b838b0da8546ce6fa50ed2e733e58c5`; latest substantive head `c30112ee594dac9af94d8e828681c9c89b082f03`.

## 2026-07-15 — Codex — Vendor-version integration expectation corrected

- **Who:** Codex, at Corey Hartin's direction.
- **What:** Corrected one synthetic PostgreSQL 17 integration expectation after final-head Linux run `29421062995` failed beyond 157 passing migration Python tests, 113 raw-inspector checks, 26 target-safety checks, and passing catalog/service integrations. The distribution-packaged PostgreSQL fixture emitted vendor-suffixed source and `pg_dump` headers, which the new evidence contract correctly reduced to `REDACTED_UNSAFE_OR_UNRECOGNIZED`; the stale integration assertion still required a visible `17.x` prefix. The check now accepts only a whole bounded PostgreSQL 17 numeric value or that exact fixed redaction token. This does not relax the production parser, expose the vendor text, or accept arbitrary version strings. The macOS job was green on the failed run; corrected Linux and macOS final-head CI remain required. Migration readiness remains RED. The retained export was not accessed, statted, hashed, read, copied, renamed, chmodded, unzipped, normalized, or inspected; no Lovable, Supabase, production, secrets, external/pre-existing database, restore, deployment, migration, export, or data movement was accessed or performed.
- **SHAs:** failed final head `74bf3b0d46ae14985e9cc99ca9d144ecd114879e`; corrected substantive head `a69f87d32b19e999ef93861b73d86be747313050`.

## 2026-07-15 — Codex — Evidence and duplicate-analysis contracts hardened

- **Who:** Codex, at Corey Hartin's direction.
- **What:** Hardened draft PR #16 in place using repository code and synthetic fixtures only. Pending-path and descriptor-bound durable validation now load provenance and evidence manifests with recursive duplicate-key and nonfinite-number rejection, enforce exact schemas plus typed scalar leaves, cross-bind tool/source/config/artifact/header/report/checksum/durable-path claims, and rebind the pending package to the descriptor-held canonical identity before publication. Fully rehashed `READY` then `BLOCKED`, `BLOCKED` then `READY`, nested duplicate, unknown `migration_readiness=GREEN`, nested scalar-object, identity, and checksum substitutions are rejected by both validators and the actual no-replace publication path without a completed or indeterminate package. Migration-duplicate analysis is now independently complete only for reviewed or explicitly non-applicable normalized classes: the supported `CREATE LANGUAGE` and named `CREATE INDEX CONCURRENTLY` forms detect duplicates, while unsupported classes, unnamed indexes, comment-obscured language syntax, unreviewed CREATE/ALTER modifiers, and lexically unprovable migration text retain only aggregate `INCOMPLETE/BLOCKED` evidence without names or SQL. Source and `pg_dump` version headers use bounded exact grammar or fixed redaction without sentinel leakage, and EXTENSION owner handling distinguishes owned, ownerless, and explicit `-` forms. Local repository-only checks passed: 157 migration Python tests, 113 raw-inspector checks, 26 target-safety regressions, eight shell syntax checks, seven Python no-write compilations, and diff checks; Linux PostgreSQL 17, application, Deno, audit, and macOS final-head CI remain required. Migration readiness remains RED. The retained export was not accessed, statted, hashed, read, copied, renamed, chmodded, unzipped, normalized, or inspected; no Lovable, Supabase, production, secrets, external/pre-existing database, restore, deployment, migration, export, or data movement was accessed or performed.
- **SHAs:** PR #16 prior head `091c3617e09eee67ba6171162805a11bf9393ced`; latest substantive head `8cbb0da107812ee23cbbd164671e0c00aa641aa2`.

## 2026-07-15 — Codex — Aggregate-only TOC evidence grammar made exact

- **Who:** Codex, at Corey Hartin's direction.
- **What:** Closed a final synthetic-review gap in draft PR #16 without touching the retained rehearsal export. An incomplete object-reference report must now match one exact LF-terminated ordered grammar: every fixed and dynamic field appears once, the full fixed-order unresolved-class ledger is present, the appended safe PGDMP-header block is exact, and metadata-entry plus data-reference counts equal the total TOC count. Planted reports containing duplicate safe-looking version, size, PostgreSQL-client, or TOC-count fields, contradictory arithmetic, reordered fields, CRLF framing, or a missing final line feed fail closed. Fully rehashed poison fixtures first pass both pending-path and descriptor-bound validation as valid packages, then mutate and rebind the report, detached hash, and evidence manifest and are rejected by both validators. Local repository-only checks passed: 140 migration Python tests, 111 raw-inspector checks, 26 target-safety regressions, eight shell syntax checks, seven Python no-write compilations, and diff checks. Linux PostgreSQL 17, application, Deno, audit, and macOS checks remain final-head CI gates. Evidence completion still means only that the retained metadata package is internally complete; `restore_planning_gate=BLOCKED` and migration readiness remains RED. This work did not access, stat, hash, read, copy, rename, chmod, unzip, normalize, or inspect the retained export and did not access Lovable, Supabase, production, secrets, or any external/pre-existing database; it performed no restore, deployment, migration, export, or data movement.
- **SHAs:** starting main `8b872882787859b87549e7f884832c624e29ead9`; original draft PR #16 substantive head `b3ad4b9379e541574972659638d5fae3ed274556`; latest substantive head `fbf78cfacd3e125021d195637cfd4aaac1353c30`.

## 2026-07-15 — Codex — Unresolvable TOC entries retain blocked aggregate evidence

- **Who:** Codex, at Corey Hartin's direction.
- **What:** Opened draft PR #16 from verified main to correct the synthetic reproduction of `report_helper_failed` / `unresolved_known_toc_entry` without touching the retained rehearsal export. PostgreSQL's editable `pg_restore --list` output has no documented lossless quoting grammar for its whitespace-separated namespace, tag, and owner fields. The report helper now accepts only a narrow class-specific ASCII hyphen rule for EXTENSION names and treats any other recognized non-exempt class whose object reference cannot be conservatively resolved as aggregate-only incomplete analysis, never as a restore-ready result. The retained report contains a total plus fixed allowlisted class counts and binds `object_reference_analysis=INCOMPLETE`, `migration_duplicate_analysis=INCOMPLETE`, and `restore_planning_gate=BLOCKED`; it omits TOC lines, names, schemas, owners, OIDs, SQL, paths, payloads, and duplicate-name details. Unknown classes, malformed TOCs, duplicate IDs, conflicting version headers, archive/hash failures, and unsafe diagnostics still fail without a normal package. Provenance format version 5, pending and descriptor-bound durable validation, report/provenance equality, stdout, and the completion marker all enforce the blocked gate. Local synthetic checks passed: 138 migration Python tests, 111 raw-inspector checks, 26 target-safety regressions, shell syntax, Python no-write compilation, and diff checks. Database-backed PostgreSQL 17, application, Deno, audit, and macOS checks remain final-head CI gates. The prior separately authorized attempt was reported to have produced no evidence package and no restore. This PR did not access, stat, hash, read, copy, rename, chmod, unzip, normalize, or inspect the retained export and did not access Lovable, Supabase, production, secrets, or any external/pre-existing database; it performed no restore, deployment, migration, export, or data movement. Migration readiness remains RED.
- **SHAs:** starting main `8b872882787859b87549e7f884832c624e29ead9`; draft PR #16 substantive head `b3ad4b9379e541574972659638d5fae3ed274556`.

## 2026-07-14 — Codex — Helper diagnostics and failed publication made unambiguous

- **Who:** Codex, at Corey Hartin's direction.
- **What:** Hardened open draft PR #15 in place without retrying or touching the retained rehearsal export. The report helper now emits exactly one fixed-order ASCII JSON diagnostic with one of eight allowlisted reasons and never relays paths, filenames, TOC lines, object names, SQL, row content, environment values, or exception text. The raw inspector privately validates that record byte-for-byte; empty, multiline, oversized, non-ASCII, malformed, extra-key, wrong-version, and unknown-reason output becomes `report_helper_failed` / `other_nonzero`. The high-level driver rejects invalid stage/reason combinations, removes partial poisoned output and every disposable/durable evidence artifact after failure, and records the changed helper against the approved execution checkout while retaining c87 only as the historical migration-input baseline. A planted combined post-link-directory-fsync/rollback-unlink failure must leave a private mode-`0400` indeterminate marker rather than a normal report; a planted closed notification descriptor proves an already committed report is not retroactively labeled failed. Local synthetic checks passed: 125 migration Python tests, 109 raw-inspector checks, 26 target-safety regressions, shell syntax, Python compilation, and diff checks. Linux PostgreSQL 17 and macOS final-head CI remain required. Migration readiness remains RED, and direct stdout compatibility remains a documented nontransactional, non-evidence stream. The retained real ZIP was not accessed, statted, hashed, listed, read, copied, renamed, chmodded, unzipped, or inspected, and no retry occurred. No Lovable, Supabase, production, secret, external/pre-existing database, restore, deployment, migration, export, or data movement was accessed or performed.
- **SHAs:** PR #15 prior head `3b27d969fe2ab0e47135b020363106559cc1c84e`; new substantive head `687ff5ee19127d0094d100d5326eaf98bf7eae26`; historical migrations-only baseline `c87a124602eb669b3ec5a3829610c6cb465d3e26`.

## 2026-07-14 — Codex — Migration inspection failures made attributable

- **Who:** Codex, at Corey Hartin's direction.
- **What:** Opened draft PR #15 to close the metadata inspector's diagnostic blind spot without retrying or touching the retained rehearsal export. Every raw-inspector failure now emits one canonical allowlisted stage/reason record; the high-level workflow accepts only that grammar and never relays child stdout/stderr, TOC text, paths, object names, secrets, or row-payload sentinels. The workflow captures the safe 11-byte PGDMP header fields before `pg_restore`, binds them to the verified inner SHA-256, reduces nonzero `pg_restore` results to reviewed reason codes, records the resolved execution-Python identity, uses isolated child Python with shell/Python startup hooks removed, and cleans the private snapshot workspace before atomic no-replace report publication. Planted regressions exercise every stage, malformed/poisoned diagnostics, cleanup and publication failures, reviewed PostgreSQL 17 reason variants, and the exact `--version`/`--list` ledger. Migration readiness remains RED: these synthetic local and CI mechanics do not prove Lovable source completeness, UI-to-backend mapping, operator observations, archive provenance, full-byte consumption by `pg_restore --list`, restore compatibility, or target readiness. The retained real ZIP was not accessed, statted, hashed, listed, copied, renamed, chmodded, unzipped, or inspected, and no retry occurred. No Lovable, Supabase, production, secret, external/pre-existing database, restore, deployment, migration, export, or data movement was accessed or performed.
- **SHAs:** starting main `4fac3414f46f56f961574cea8ec99f1c439e7112`; draft PR #15 substantive head `65281d0597c8339882f2e70d8f0119dc5f1948d5`; unchanged historical helper/migration baseline `c87a124602eb669b3ec5a3829610c6cb465d3e26`.

## 2026-07-14 — Codex — Appended-byte verification ceiling assertion corrected

- **Who:** Codex, at Corey Hartin's direction.
- **What:** Corrected draft PR #14's synthetic real-PostgreSQL integration assertion after CI empirically showed PostgreSQL 17 accepts a valid `pg_dump -Fc` archive with appended bytes. The prior regex looked for “inner” before “byte” and therefore failed to recognize the exact checked-in warning; the harness now requires that warning as an exact fixed string. This does not claim byte-consumption proof: whole-file hashing binds the input, while `pg_restore --list` still does not prove it consumed every inner byte. Migration readiness remains RED. No retained artifact, Lovable, Supabase, production, secret, external/pre-existing database, restore, deployment, migration, export, or data movement was accessed or performed.
- **SHAs:** failed evidence run `29379136414` on prior final head `3e1e0eba56f51895af19d05e4e29d38c829e2c59`; corrected substantive head `a4034712139b49416ceca3511765767f53455ba3`; unchanged inspector/helper/migration baseline `c87a124602eb669b3ec5a3829610c6cb465d3e26`.

## 2026-07-14 — Codex — Real PostgreSQL export-workflow CI harness isolated

- **Who:** Codex, at Corey Hartin's direction.
- **What:** Corrected only draft PR #14's synthetic PostgreSQL 17 high-level test harness after CI exposed readonly shell-name collisions and a non-guaranteed clean execution checkout. The harness now passes the audited real-`pg_restore` path and invocation ledger through distinct environment names, clones the exact test HEAD into an isolated clean checkout, and runs the complete ZIP and direct-`PGDMP` drivers there without weakening the production clean-worktree guard. This correction does not broaden the workflow or its proof: migration readiness remains RED, inspection still requires a separately approved exact checkout and external artifact identity, and the test proves only synthetic local archive handling plus the permitted `pg_restore --version`/`--list` calls. No retained artifact, Lovable, Supabase, production, secret, external/pre-existing database, restore, deployment, migration, export, or data movement was accessed or performed.
- **SHAs:** failed final-head evidence run `29378647583` on prior final head `f713116292ab6174af5c4ea5c6c7703d6cc58387`; corrected substantive head `e4120f0d731a17850890d5bbc20ad8b26144c646`; unchanged inspector/helper/migration baseline `c87a124602eb669b3ec5a3829610c6cb465d3e26`.

## 2026-07-14 — Codex — Lovable ZIP evidence workflow hardened for re-review

- **Who:** Codex, at Corey Hartin's direction.
- **What:** Hardened open draft PR #14 without inspecting the retained export. The checked-in workflow now requires externally approved canonical outer filename, exact byte size, SHA-256, and private evidence-store root before run creation or `pg_restore`; binds the repository project ref and ZIP member/UI object name; rejects FIFO/symlink/ownership/mode/path substitutions; and distinguishes expected identity from descriptor-observed identity in provenance. Durable evidence is copied, hashed, mode-bound, and fsynced through held directory descriptors, published with platform-native no-replace rename, then postcommit-validated before the exact completion marker is written. Any postcommit validation failure loses completion status and gains a durable `EVIDENCE_INDETERMINATE` marker; existing outputs are never overwritten or deleted. A fixed bounded guard admits only `pg_restore --version` and `--list`, kills descendants on failure even after the leader exits, and caps time/output. Planted regressions cover substitutions, source/config/member mismatches, private-store violations, collisions, late root/canonical changes, process escapes, partial failure, timeline errors, and completion/indeterminate separation. Real synthetic `pg_dump -Fc` ZIP/direct integration is required in PostgreSQL 17 Linux CI, and a macOS job exercises `renameatx_np`. Migration readiness remains RED. The workflow still cannot prove Lovable source completeness or mapping, operator observations, restore safety, every direct-PGDMP byte consumed by `pg_restore`, or target readiness; final filesystem I/O ambiguity is an explicit manual-quarantine ceiling. No retained artifact, Lovable, Supabase, production, secret, external/pre-existing database, restore, deployment, migration, export, or data movement was accessed or performed; CI database fixtures are isolated and synthetic.
- **SHAs:** PR #14 prior head `25ed0d22cb94ce36f4f3984e42cc97ac40858b77`; new substantive head `ecd719caed4fa1c5757fe36c305313d952ff0f24`; unchanged inspector/helper/migration baseline `c87a124602eb669b3ec5a3829610c6cb465d3e26`.

## 2026-07-14 — Codex — Lovable ZIP-envelope inspection prepared for review

- **Who:** Codex, at Corey Hartin's direction.
- **What:** Opened draft PR #14 from verified main `ecdef62656c793b62611d89be22b09d15a03e74d` to add a strict ZIP normalizer ahead of the unchanged raw-`PGDMP` inspector. The workflow treats the downloaded ZIP as canonical outer evidence and its sole verified `PGDMP` member as a distinct derived artifact; enforces exact framing, safe regular-member identity, bounded streaming, CRC/length/hash checks, disk headroom, private fixed-name output, fail-closed cleanup, and atomic OS-level no-replace evidence publication; preserves direct-`PGDMP` support; and records separate outer/member/inner/report/provenance identities. Structured provenance keeps the retained rehearsal's unobserved initiation explicit, separates availability from completion, and remains `INCOMPLETE` / `REVIEW_REQUIRED`; future/final profiles require observed initiation. Synthetic adversarial tests execute the complete checked-in workflow with a controlled fake `pg_restore` and prove only `--version` and `--list` are invoked. Migration readiness remains RED. The authorized retained export and all identifying metadata were untouched; no Lovable, Supabase, production, secrets, remote/pre-existing database, restore, deployment, migration, export, or data movement was accessed or performed.
- **SHAs:** PR #14 substantive head `19c8e4187311847bf5c48ffe4a3804b7934cc850`; unchanged inspector/helper/migration baseline `c87a124602eb669b3ec5a3829610c6cb465d3e26`.

## 2026-07-14 — Codex — External migration procedure approval required

- **Who:** Codex, at Corey Hartin's direction.
- **What:** Removed draft PR #13's self-referential procedure-approval claim. The metadata-only workflow now requires an externally supplied full `APPROVED_EXECUTION_CHECKOUT_SHA` with no default, verifies that it names an available commit and exactly equals the runtime-resolved HEAD before creating the run directory or invoking `pg_restore`, and separately records the approved checkout, observed execution checkout, committed README blob, marked fenced-workflow SHA-256, informational historical origin, and unchanged inspection-tool/migration baseline. A synthetic clean committed-descendant test executes the descendant's own newly committed workflow, proves the prior approval pin fails without creating evidence or calling fake `pg_restore`, then proves the descendant proceeds only when its exact HEAD is explicitly approved; missing, empty, malformed, and unavailable approval pins also fail at the same boundary. The local evidence proves supplied-pin equality and checked-out content identity, not who authorized the pin, an independently captured shell-input stream, a real Lovable archive, source completeness, remote state, or restore compatibility. No Supabase or Lovable project was accessed or changed; no export, restore, remix, database query, secret, deployment, production invocation, or production data access or movement occurred.
- **SHAs:** PR #13 prior head `f871c210925072dcd7d510d976d192b19b0bada0`; new substantive head `df0a28d0409b6123e74179f41c625f5b20a168f7`.

## 2026-07-14 — Codex — Migration evidence provenance separated

- **Who:** Codex, at Corey Hartin's direction.
- **What:** Corrected draft PR #13's metadata-only evidence procedure so the procedure-origin commit (`e4eed4a21049d274738110710a468e265c2893d2`), unchanged inspector/helper/migration-baseline commit (`c87a124602eb669b3ec5a3829610c6cb465d3e26`), and exact execution checkout are independently validated and recorded. Inspection now fails before invoking `pg_restore` when the inspector, helper, or tracked migrations differ from the tool baseline, when ordinary or ignored untracked migrations exist, or when any provenance SHA is wrong, missing, or malformed. A synthetic end-to-end test executes the complete checked-in Bash workflow in a temporary Git checkout against a local synthetic archive and controlled fake `pg_restore`, with planted failures for every new guard. This proves the local workflow and fail-closed mechanics only; it does not inspect a real Lovable export, prove Lovable source completeness or configuration, establish remote state, or validate restore compatibility. No Supabase or Lovable project was accessed or changed; no export, restore, remix, database access, secret, deployment, production invocation, or production data access or movement occurred.
- **SHAs:** PR #13 prior head `f4710503432d6bc42cbd7bdb2a8d4ad9a316cfad`; new substantive head `b6084905d2a04c752c94545d592dccc29226d86a`.

## 2026-07-14 — Codex — Migration inspection evidence package bound

- **Who:** Codex, at Corey Hartin's direction.
- **What:** Closed the metadata-only execution-handoff evidence gap in draft PR #13 without adding a restore or database path. The checked-in template preserves the canonical archive outside the ignored worktree, verifies its disposable working copy, retains external before/after checksum sidecars, requires exactly one matching archive SHA in the inspector report, hashes the report separately, and emits a provenance manifest with the exact source project, observed UTC times separated from unproven Support claims, operator, reviewed/tool Git SHA, original archive metadata, and report identity. Synthetic tests parse and execute the documented evidence-generation/validation snippets and plant external/report mismatch and duplicate-report-hash failures. No Supabase or Lovable project was accessed or changed; no export, restore, remix, secret, deployment, production invocation, database, or production data was accessed or moved.
- **SHAs:** PR #13 prior head `c87a124602eb669b3ec5a3829610c6cb465d3e26`; new substantive head `e4eed4a21049d274738110710a468e265c2893d2`.

## 2026-07-14 — Codex — Migration PostgreSQL CI false-green closed

- **Who:** Codex, at Corey Hartin's direction.
- **What:** Hardened draft PR #13 after independently confirming that successful CI run `29341450013` logged three unavailable-`rg` errors while the catalog and service harnesses still printed `PASS`. Replaced those checks with preflighted fixed-`grep` assertions that accept only exit 1 as no-match, removed optimization-removable production Python assertions, and added a shared fail-closed target boundary before destructive synthetic fixtures. Direct execution now requires the canonical local PostgreSQL Unix socket; container execution requires the explicit test prefix, a local effective Docker endpoint, and a test-only label. Both paths require fixture opt-in, a `migration_verify_*` database, PostgreSQL 17, and exact local-socket identity. Twenty-six negative guard cases prove remote/non-test targets, identity mismatches, missing dependencies, assertion-command errors, and planted leaks fail without sending fixture SQL. No Supabase or Lovable project was accessed or changed; no export, restore, remix, secret, deployment, production invocation, or production data was accessed or moved.
- **SHAs:** PR #13 prior head `df10c9f7fe12d6e0a40391bfd14e41b5b130206f`; new substantive head `d84c7a4153ba55d7574d466383b82ec275082f28`.

## 2026-07-14 — Codex — Migration rehearsal hardened and runtime state caught up

- **Who:** Codex, at Corey Hartin's direction.
- **What:** Hardened draft PR #13 without changing its migration architecture: eliminated local manifest false greens, fingerprinted resolved Edge Function deployment closures/effective JWT configuration, replaced ambiguous catalog evidence with strict typed fingerprints and a deterministic converter, bound dump TOCs to immutable archive snapshots, added PostgreSQL 17 integration coverage, and strengthened Remix connection, Storage final-sync, target-lifecycle, and enforced maintenance/write-fence gates. `.lovable/plan.md` is now visibly rejected. This entry also records the limited July 12 production `version` response observation for merged PR #11 and open draft PR #12's unresolved deployment prerequisites. No Supabase or Lovable project was accessed or changed; no export, restore, remix, secret, deployment, production invocation, or production data was accessed or moved.
- **SHAs:** PR #13 starting hardening head `e9ddd60e695b0d8bca405e514030d13b14f6daed`; hardening substantive head `6b9ca8ea617843acb15797e8b964e54e5487d78c`. PR #12 open draft head `a70ea4e1e3cdaa588546cbd3730481ae85b664cd`; PR #11 merge `447c00f6bdfe988053e8e9ee650a834679a1a7fc` and observed version source `5e071bee29751f549dc3ae3f5308e0d81005be72`.

## 2026-07-14 — Codex — Supabase migration rehearsal prepared for review

- **Who:** Codex, at Corey Hartin's direction.
- **What:** Opened draft PR #13 to replace the rejected custom bridge architecture with a repository-derived migration inventory, 22-phase export/remix rehearsal and cutover runbook, local read-only dump inspection, fail-closed manifest comparison, verification templates, and a precise empirical/Lovable-confirmation backlog. The implementation treats Lovable Support's path and constraints as reported but not yet proven, commits synthetic fixtures only, and deliberately omits a final restore command until a real export TOC is inspected under separate authorization. No Supabase or Lovable project was created, queried, remixed, changed, or deployed; no export, secret, production system, or production data was accessed or moved.
- **SHAs:** PR #13 substantive head `5b56593a491227bc690751fffee43d4c3658317d`.

## 2026-07-12 — Codex — Production runtime version attestation prepared for review

- **Who:** Codex, at Corey Hartin's direction.
- **What:** Opened draft PR #11 with a side-effect-free public `version` Edge Function, shared bundle-bound release metadata, fail-closed runtime identity response, focused Deno/security tests, CI coverage, and a dispatch-only production verifier. Independent-review hardening added strict release/commit format and binding validation plus main-ancestry and project-scoped deployment-ID verification. Release `edge-20260712-5e071bee2975` is bound to substantive hardening commit `5e071bee29751f549dc3ae3f5308e0d81005be72`. No merge or deployment was performed.
- **SHAs:** PR #11 hardened substantive head `5e071bee29751f549dc3ae3f5308e0d81005be72`.

## 2026-07-12 — Codex — State logging mechanics refined

- **Who:** Codex, at Corey Hartin's direction.
- **What:** Merged PR #9 as the final standalone log-only PR under the old pattern. In PR #10, refined the shared protocol so CURRENT STATE and SESSION LOG updates travel with the substantive work PR before merge, standalone log PRs are prohibited, non-PR state changes are batched into an open or next work PR, and Claude hands drafted entries to Codex for inclusion when Claude cannot push.
- **SHAs:** PR #9 head `c7caf99934e5eeede9f7fe17d2c19e715c59ee2f`; PR #10 substantive head `232a43b09b30c6011d0a1adea29db7a2f54ad122`.

## 2026-07-12 — Codex — Frontend type-safety restored / CI blind spot closed

- **Who:** Codex (rebase, verification, and merge).
- **What:** Merged PR #5 (`03d77dd`), removing the postinstall type-safety bypass, changing `typecheck` to full-project `tsc -b`, and fixing 28 `RejectExcessProperties` errors. CI now catches frontend type errors. Frontend-only; no Supabase Edge Function files were touched.
- **SHAs:** PR head `03d77dd93a515b77cccd6d323bce1c5e2cf7bbcb`; merge commit `7c4b8eb39301092c652923ffea3f7d81dfa9d7c3`.

## 2026-07-11 — Claude + Codex + Lovable — Strategy trust layer shipped to prod (night)

- **Who:** Claude (coordinator/verification), Codex (build), and Lovable (production database/deploy).
- **What:** Shipped and verified live PR #4 (`040751b`), the semantic-reduction and competitive-industry stack with five precedence conflicts resolved, CARD restored, a single citation authority, and explicit count detection; PR #5, which cleared frontend type debt and removed the postinstall type-safety bypass; and PR #6 (`7ee1ebe6` / `41b539ed`), which added competitive-citation integrity, the fabrication guard (⚠ UNVERIFIED verified in production), the classifier explicit-intent override, and source-claim integrity. Corrected `territory_profile` in production to Strategic AE / 13 accounts / $1.4M.
- **SHAs:** `040751b`, `7ee1ebe6`, `41b539ed`.
- **Deploy:** Deployed edge functions with `supabase functions deploy`; `strategy-chat` is live from `41b539ed`; the frontend was published. Production remains on Lovable-managed Supabase and has not been migrated.
- **System of record:** Established `docs/STATE.md` in this session through merged PR #7 as repository-authoritative. Todoist is the action layer; Drive is the read-only human mirror.
- **Open:** S-R1 retrieval relevance, S-R2 Sources panel, S-R3 prompt budget (17,491/17,500), Supabase migration, product-layer wiring, and Web Research reconciliation. See §D and Todoist.
- **Provenance:** Code verified by direct repository read; CI green on `41b539ed`; the fabrication guard and competitive retrieval verified live in production. The 17,491 prompt-budget figure is Codex's Deno-matrix result and was not rerun in the coordinating session.

## 2026-07-11 — Codex — Unified system of record initialized

- **Who:** Codex, at Corey Hartin's direction.
- **What:** Created repository-authoritative `docs/STATE.md`, seeded CURRENT STATE from Todoist v21, and established the repo/Todoist/Drive ownership model. Deleted the isolated Todoist connector test task `6h54MF6x83jmghcg`; no live roadmap task was changed.
- **Related SHAs:** `040751b`, `41b539ed`, `7ee1ebe6`.
